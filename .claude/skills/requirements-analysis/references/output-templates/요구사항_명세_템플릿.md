# '<프로젝트명>' 요구사항 명세

<!-- 사용법: Phase 1 워크플로우의 결과를 아래 섹션에 누적 저장한다. 매 스토리 검토가 끝나는 시점에 갱신하며, 매번 파일 전체를 다시 쓰지 마라. -->
<!-- 양식 가드레일·결정 분기 메커니즘·도메인 사전 풍부화 룰 등 상세는 references/요구사항_명세_작성_가이드.md 참조. -->

본 문서는 '<프로젝트명>'의 기능에 대한 합의 사항을 기록한다. 한 줄 요청만으로는 드러나지 않는 전제·예외·정책을 풀어 적고, 트레이드오프가 있는 결정은 [핵심 의사결정 분기](#핵심-의사결정-분기)에 그 이유와 함께 남긴다.

<!-- 서문은 선택. 사용자가 원할 때 추가. 정의형 톤("기록한다"로 마무리). -->

---

## 도메인 및 용어 정의

<!-- 도메인별 표 병합. 도메인 자체 + 본질 속성(이름·길이·범위·중복 정책 등)을 함께 적는다. -->
<!-- 메타 필드(isActive·createdAt·updatedAt·deletedAt·운영 메모)는 사전에서 제외 — 도메인 본질이 아니라 운영·기술 메타. -->
<!-- 영어명 컨벤션: 도메인 자체와 본질 속성 모두 PascalCase (`Brand`, `Name`, `Price`, `UnitPrice`, `TotalPrice`). -->
<!-- 값 객체(Price·Stock·Quantity·UnitPrice 등)는 별도 섹션 없이 해당 도메인 표에 같이. -->

### <도메인 한글> (<도메인 영어>)

| 한글 | 영어 | 의미 |
|---|---|---|
| <도메인 한글> | `<영어>` | <의미·핵심 제약> |
| <본질 속성 한글> | `<영어>` | <의미·길이/범위 등 제약> |

---

## 액터 정의

<!-- 첫 등장 시 추가. 인증 헤더 등 식별 방법도 함께 명시. -->

| 한글 | 영어 | 의미 |
|---|---|---|
| 회원 | `User` | <식별 방법, 예: 헤더 `X-Loopers-LoginId`·`X-Loopers-LoginPw`로 식별.> |
| 비회원 | `Guest` | 가입하지 않았거나 로그인하지 않은 상태로 시스템을 이용하는 사용자. |
| 관리자 | `Admin` | <식별 방법> |

---

## <도메인 한글> (<도메인 영어>)

<!-- 각 도메인 그룹 헤더 아래에 해당 도메인의 유저 스토리들을 순서대로. 도메인 그룹 사이는 `---`로 구분. -->

### <도메인 약어>-<번호>. <유저 스토리 한 문장>

<!-- 식별자 예: BRD-1, PRD-2, LIK-3, ORD-1. 도메인별로 연속 번호. 스토리 ID는 한 번 부여하면 재정렬하지 않음. -->

**전제 조건**
- <인증 상태·대상 자원의 운영 상태·요청 형식 등 호출 전에 만족해야 하는 조건>

**기능 요구사항**
- Main Flow
    1. <시스템 관점의 단계별 동작. 마지막 응답까지 끝맺기.>
- Alternate Flow (선택)
    - <Main Flow와 다른 자연스러운 흐름>
- Exception Flow
    - <어느 입력 / 어느 상태 → 어떤 응답. 응답 어휘는 "해당 자원을 찾을 수 없다 / 자원 충돌 / 입력 검증 실패 / 인증 실패"로 통일.>

**비즈니스 규칙** (선택, 자명하면 생략)
- <중복 정책·정렬·페이지 크기·노출 범위·계산 산식 등>

**비기능 요구사항** (선택, 해당 없으면 생략)
- <동시성·응답 시간·보안·일관성 등. 추상적 표현 X, 도메인 의미로.>

**관련 API**
- `<HTTP METHOD> <URL>`
<!-- 비즈니스 규칙에 페이지네이션·정렬·필터가 있으면 URL에 쿼리 파라미터도 함께. 예: `GET /api/v1/products?brandId={brandId}&sort=latest&page=0&size=20`. -->
<!-- 회원/비회원 API는 `/api/v1/...`, 관리자 API는 `/api-admin/v1/...`. -->

---

## 핵심 의사결정 분기

<!-- 결정 분기 감지 시 사용자 동의 후 추가. 단순 표현 교정·자명한 선택은 결정 분기로 보지 않음. -->
<!-- 결정 번호는 1부터 연속. 결정 폐기 시 본문에서 완전히 제거하고 번호 재정렬. 다른 스토리의 "(결정 N 참조)"도 함께 갱신. -->

### 결정 <번호>. <분기 제목>

**문제**: <무엇이 결정 대상이었는지 한 문단>

| 안 | 접근 | 트레이드오프 |
|---|---|---|
| **A (채택)** | <접근 한 줄> | <영향도: 구현 부담·확장성·동시성·운영 비용 중 핵심 한 줄> |
| B | <접근> | <영향도> |
| C | <접근> | <영향도> |

<!-- 트레이드오프 열은 필수. 영향도 없이 단순 나열만 하면 후속에서 의사결정 근거 추적 불가. -->
<!-- 결정 분기의 트레이드오프 열에서는 구현 어휘(원자적 조건부 갱신·비관 잠금·캐시 미스 폴백 등) 허용. 본문에서는 금지. -->

**사유**: <왜 채택안인지>

**추후 개선**: (선택) <후속 라운드 진화 자리>

server:
  # pg-simulator는 결제 callbackUrl이 http://localhost:8080 으로 시작할 것을 요구한다(03 §3.8).
  # PaymentFacade.CALLBACK_URL과 일치해야 콜백 수신이 동작하므로 기본값에 의존하지 않고 명시한다.
  port: 8080
  shutdown: graceful
  tomcat:
    threads:
      max: 200 # 최대 워커 스레드 수 (default : 200)
      min-spare: 10 # 최소 유지 스레드 수 (default : 10)
    connection-timeout: 1m # 연결 타임아웃 (ms) (default : 60000ms = 1m)
    max-connections: 8192 # 최대 동시 연결 수 (default : 8192)
    accept-count: 100 # 대기 큐 크기 (default : 100)
    keep-alive-timeout: 60s # 60s
  max-http-request-header-size: 8KB

spring:
  main:
    web-application-type: servlet
  application:
    name: commerce-api
  profiles:
    active: local
  config:
    import:
      - jpa.yml
      - redis.yml
      - kafka.yml
      - logging.yml
      - monitoring.yml

# 좋아요 카운터 비동기 집계 토픽 (commerce-streamer가 컨슈머). key=productId 로 같은 상품은 같은 파티션.
like-events:
  topic: catalog.like-changed.v1
  partitions: 3

# 외부 PG(pg-simulator) 연동
pg-simulator:
  base-url: http://localhost:8082

# 회복성 — pg-simulator는 동기 요청의 40%를 일시 500으로 떨군다.
resilience4j:
  retry:
    instances:
      pgSimulator:
        max-attempts: 4              # 최초 1 + 재시도 3 (0.6^4 ≈ 13% 까지 실패 누적 확률을 낮춤)
        wait-duration: 200ms
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
        retry-exceptions:
          - feign.RetryableException        # 연결/타임아웃
          - feign.FeignException.InternalServerError   # 주입된 500
  circuitbreaker:
    instances:
      pgSimulator:
        sliding-window-type: COUNT_BASED
        sliding-window-size: 20
        minimum-number-of-calls: 10
        failure-rate-threshold: 70           # 40% 정상 실패보다 높게 — 진짜 장애에만 개방
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 3

springdoc:
  use-fqn: true
  swagger-ui:
    path: /swagger-ui.html

---
spring:
  config:
    activate:
      on-profile: local, test

---
spring:
  config:
    activate:
      on-profile: dev

---
spring:
  config:
    activate:
      on-profile: qa

---
spring:
  config:
    activate:
      on-profile: prd

springdoc:
  api-docs:
    enabled: false
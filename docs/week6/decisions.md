요구사항을 분석하고 기존 설계의 변경 지점을 짚어보겠습니다. 비동기 PG가 도입되면서 시스템의 가장 핵심적인 트랜잭션
설계가 완전히 뒤바뀌어야 하는 상황입니다.
──────
### 1️⃣ 문제 상황 다시 정의하기 (요구사항의 재해석)
• [사용자 관점] 결제 요청 후 결과가 나오기까지 1~5초가 걸릴 수 있으며, 중간에 통신이 끊겼을 때 내 결제가 성공했는지
실패했는지 직관적으로 알기 어렵다.
• [비즈니스 관점] PG사 연동 과정에서 성공 70%, 실패 30%(한도초과/잘못된 카드 등)가 발생한다. 외부 시스템의 실패로
인해 우리 시스템의 재고나 쿠폰이 영구적으로 묶이는 일(가선점 상태 고착)이 발생해서는 안 되며, 반대로 결제가 안
되었는데 주문이 성공 처리되는 일도 없어야 한다.
• [시스템 관점] 가장 큰 문제입니다. 기존 설계는 **"단일 트랜잭션 내에서 동기식으로 PG를 호출"**하는 구조였습니다.
처리가 최대 5초 지연되는 PG를 트랜잭션 안에서 호출하게 되면, 단 몇십 건의 주문만으로도 DB 커넥션 풀이 고갈되고
비관적 락이 장기간 점유되어 시스템 전체가 마비(장애 확산)됩니다.
──────
### 2️⃣ 애매한 요구사항 드러내기 (질문 및 확인 필요 사항)
코드를 작성하기 전, 다음 정책들에 대한 명확한 결정이 필요합니다.
1. [경계 질문] 주문과 결제의 분리
   • 제공해주신 결제 API( POST /api/v1/payments ) 스펙을 보면  orderId 를 입력받습니다. 이는 과거의 한 번에
   처리하던  /checkout  API와 달리, **"1. 주문 생성(재고 차감)"**과 "2. 결제 요청" API가 물리적으로 분리되어야
   함을 의미합니다. 이렇게 두 단계를 나누는 것이 맞을까요?
2. [정책 질문] PG사 콜백 누락(유실) 시 보정 정책
   • PG사에서 성공/실패 처리를 했지만, 우리 서버로 콜백( callbackUrl )을 보내주지 않거나 네트워크 단절로 유실될 수
   있습니다. 이때 어떤 방식으로 결제 상태를 복구할 것인지 정해야 합니다.
3. [정책 질문] 결제 "요청" 타임아웃 시의 처리
   • 콜백을 기다리기도 전에, 최초 결제 요청( POST /api/v1/payments ) 자체가 타임아웃(100~500ms 지연)으로 끊어질 수
   있습니다. 이때 즉시 우리 시스템의 주문을 취소할 것인지, 아니면 PG사에 요청이 도달했을 가능성을 고려해 대기할
   것인지 결정해야 합니다.
   ──────
### 3️⃣ 해결을 위한 선택지 제안

위 질문에 대해 다음과 같은 선택지를 제안합니다. (추천 옵션을 먼저 제시합니다)
• Q2. 콜백 누락 보정 정책
• 선택지 A (Recommended): 스케줄러를 통한 자동 폴링(Polling) 동기화
• 일정 시간(예: 3분)이 지나도 상태가  READY 인 결제 건들을 Batch가 수집하여, PG의  결제 정보 확인 API(GET)
를 호출해 상태를 맞춥니다.
• 영향도: 사용자의 개입 없이 시스템이 스스로 일관성을 맞추지만, 스케줄러 구현이 추가됩니다.
• 선택지 B: 수동 단건 동기화 API 제공
• 사용자가 "결제 상태 새로고침" 버튼을 누르거나, 어드민이 수동으로 갱신합니다.
• 영향도: 구현은 단순하지만 사용자 경험이 떨어지고 붕 뜬 데이터가 남을 수 있습니다.
• Q3. 요청 타임아웃 시 처리 정책
• 선택지 A (Recommended): 타임아웃 시 즉시 취소하지 않고, 확정 후 보상 트랜잭션
• 타임아웃이 발생해도 PG쪽엔 요청이 접수되어 나중에 콜백으로 성공이 날아올 수 있습니다. 즉시 취소하지 않고
주문을  PENDING 으로 유지한 뒤, 콜백이나 폴링(배치)을 통해 상태를 확정받습니다.
• 영향도: 고객이 재결제를 시도할 때 다소 혼란이 있을 수 있으나 결제 사고(돈은 나갔는데 주문은 취소됨)를
원천 차단합니다.
──────
### 4️⃣ 개념 모델 및 트랜잭션 설계
• 액터: 사용자, 외부 PG 시스템(Webhook Callback)
• 핵심 도메인:
• 주문(Order): 생성 시  PENDING . 콜백 결과에 따라  COMPLETED  또는  CANCELED .
• 결제(Payment): 요청 시  READY . 콜백/폴링에 의해  APPROVED  또는  FAILED .
• 보조 시스템: 보상 트랜잭션 (결제 실패/취소 시 재고 및 쿠폰을 복구하는 내부 로직)
──────
### 5️⃣ 다이어그램으로 설계 흐름 검증
이 다이어그램은 왜 기존의 단일 트랜잭션 설계가 폐기되어야 하는지, 그리고 네트워크 요청과 DB 트랜잭션을 어떻게
분리해야 시스템이 멈추지 않는지를 검증하기 위해 작성되었습니다.

    sequenceDiagram
        title 비동기 결제 처리를 위한 분리된 트랜잭션 흐름
        actor User
        participant Facade as Order/Payment Facade
        participant DB
        participant PG as PG Simulator

        %% 1단계: 주문 생성 (트랜잭션 분리)
        rect rgba(0, 128, 0, 0.1)
            Note right of Facade: 트랜잭션 1: 주문 생성 및 사전 차감 (매우 짧음)
            User->>Facade: 1. 주문 생성 요청 (POST /api/v1/orders)
            Facade->>DB: 재고 비관적 락 & 차감
            Facade->>DB: 주문 및 쿠폰 상태 반영 (PENDING)
            Facade-->>User: orderId 반환
        end

        %% 2단계: 결제 요청 (DB 락 없음)
        rect rgba(0, 0, 255, 0.1)
            Note right of Facade: 트랜잭션 2: 결제 정보만 임시 저장 (매우 짧음)
            User->>Facade: 2. 결제 요청 (POST /api/v1/payments)
            Facade->>DB: 결제 내역 저장 (READY)
        end

        Note right of Facade: ⚠️ 중요: 외부 통신 중에는 DB 트랜잭션을 물고 있지 않음
        Facade->>PG: 외부 PG사 결제 요청 (timeout 500ms)

        alt PG 요청 타임아웃 / 지연
            PG--xFacade: Timeout Exception
            Facade-->>User: "결제가 진행 중입니다" (이후 Batch가 복구)
        else PG 요청 정상 접수
            PG-->>Facade: 접수 완료 응답
            Facade-->>User: "결제가 진행 중입니다"
        end

        %% 3단계: 비동기 콜백 (상태 확정 및 보상 트랜잭션)
        rect rgba(255, 0, 0, 0.1)
            Note right of Facade: 트랜잭션 3: 결제 결과 반영 및 보상 로직 (단기)
            PG->>Facade: 3. 비동기 Callback API 호출

            Note right of Facade: 위조 방지 교차 검증 (선택사항)
            Facade->>PG: 결제 정보 확인 (GET /api/v1/payments/...)
            PG-->>Facade: 실제 상태 응답

            alt 결제 성공
                Facade->>DB: 결제 APPROVED, 주문 COMPLETED
            else 결제 실패 (한도초과 등)
                Facade->>DB: 결제 FAILED, 주문 CANCELED
                Note right of Facade: 🔄 보상 트랜잭션 실행
                Facade->>DB: 차감된 재고 복구, 쿠폰 상태 원복
            end
        end

#### 💡 다이어그램 해석 포인트

1. 초록색, 파란색, 빨간색 박스는 각각 별개의 독립적인 DB 트랜잭션입니다. 기존처럼 외부 API 호출을 감싸는 거대한
   트랜잭션은 없습니다.
2. 결제 요청을 던진 후, 시스템은 바로 응답을 내리고 콜백을 대기합니다. PG가 5초 동안 멈춰있어도 우리 서버의 DB
   커넥션은 이미 반환된 상태이므로 다른 유저의 요청에 영향을 주지 않습니다.
3. 결제가 실패할 경우, 선점했던 재고와 쿠폰을 되돌려 놓는 **보상 트랜잭션(Compensating Transaction)**이 빨간 박스
   내부에서 매우 중요하게 동작합니다.
   ──────
### 6️⃣ 기존 설계 문서( /docs/week2 )에서 변경되어야 할 부분
위의 요구사항 분석에 따라 기존 문서들은 다음과 같이 수정되어야 합니다.

1.  01-requirements.md
    • [삭제] 4. 기술적 제약 사항의 "단일 트랜잭션 기반 주문/결제 처리" 조항을 삭제합니다.
    • [추가] 분산 환경에서의 보상 트랜잭션 및 타임아웃, 콜백 유실에 대한 스케줄러(Batch) 폴링 정책에 대한 요구사항
    명시가 추가되어야 합니다.
    • [변경] 단일 주문 API였던  /api/v1/orders/checkout 이 → 주문 생성( /orders )과 결제( /payments ) 두 개의 API로
    분리되어야 합니다.
2.  02-sequence-diagrams.md
    • 현재의  주문 요청 API 시퀀스 다이어그램 을 삭제하고, 위에서 제시한 3단계로 쪼개진 시퀀스 다이어그램으로 대체해야
    합니다.
    • 결제 Callback API에 대한 시퀀스 다이어그램이 새로 추가되어야 합니다.
3.  03-class-diagram.md

• 보상 트랜잭션을 수행하기 위해  Stock  및  CouponIssue  도메인에 롤백을 위한 메서드( restore() ,  cancel() )가 잘
정의되어 있는지 확인하고 책임 관리를 명시합니다. (현재 명시되어 있으나 사용처가 달라집니다)

4.  04-erd.md

• 구조적인 변경은 거의 없습니다만,  PAYMENTS  테이블의  transaction_id  (PG사가 내려주는 키) 값을 콜백을 받을 때
매칭할 수 있도록  READY  단계에서 어떻게 부여받고 업데이트할지(PG 요청 시점에 채울지, 콜백 시점에 채울지) 세부 정책
검토가 필요합니다.
──────
### ⚠️ 설계의 잠재 리스크 (Risk & Trade-off)

이 설계를 도입할 때 피할 수 없는 리스크를 분명히 짚고 넘어가야 합니다.

1. 데이터 정합성 깨짐의 위험 (보상 트랜잭션 실패)
   • 비동기 콜백을 받아 결제 실패 처리를 할 때, DB 트랜잭션 예외나 서버 다운으로 "보상 트랜잭션(재고 복구)"이
   실패하면 영구적으로 재고가 증발합니다. 이벤트 기반 아키텍처(Spring Event + Outbox Pattern 등)로 재시도를
   보장할지, 아니면 단순  try-catch  후 실패 시 알럿(Alert)을 띄우고 수동으로 복구할지 수준(Trade-off)을 결정해야
   합니다.
1. 결제 요청 중복 (멱등성 문제):
   • 클라이언트가 결제 요청을 더블 클릭하거나 재시도할 때, 서버는 재고만 2번 깎거나 외부 PG에 요청을 2번 보낼
   위험이 있습니다.  Idempotency Key (멱등성 키)나 주문 상태( PENDING 인지 검사)를 기반으로 방어 로직을 촘촘히
   세워야 합니다.
2. 콜백의 동시성 경합 (Race Condition):
   • PG사에서 Callback을 재시도하여 동시에 2건이 들어오거나, 사용자가 '결제 확인' API를 누르는 시점과 Callback이
   들어오는 시점이 겹칠 수 있습니다. 이를 막기 위해 주문 정보를 갱신할 때 반드시 상태 기반 Optimistic Lock 혹은
   주문 ID 기준 DB 락이 필요합니다.
   위 내용에 대해 어떻게 생각하시나요? 트랜잭션 분리와 콜백/폴링 혼합 모델로 문서를 수정하는 방향으로 진행해도
   괜찮을지, 선택지 중에서 선호하시는 방향이 있다면 알려주시기 바랍니다!

### 1. 주문과 결제 API 분리 여부 (트랜잭션 경계 설계)

[문제 상황 및 분석]
현재  /docs/week2 의 요구사항과 시퀀스 다이어그램은 주문 생성, 재고 비관적 락 차감, 그리고 PG사 연동 처리가 단일
API ( /api/v1/orders/checkout ) 및 거대한 단일 DB 트랜잭션 내에서 한 번에 이루어지도록 설계되어 있습니다.
하지만 연동해야 할 PG 시스템은 최대 1초~5초의 처리 지연이 발생하는 비동기 환경입니다. 이 구조를 유지할 경우, DB
커넥션을 5초 동안 물고 있게 되어 단 몇십 건의 결제 요청만으로도 커넥션 풀이 고갈되고 재고 비관적 락으로 인해 시스템
전체에 병목이 발생합니다.

[선택지]

• 선택지 A (추천): 주문 API와 결제 API의 물리적 분리
• 기존 단일 설계를 폐기하고, 두 단계로 나눕니다.
• 1단계: 주문 생성 API ( POST /api/v1/orders ) - 재고를 차감하고 주문을  PENDING  상태로 DB에 짧은 트랜잭션으로
저장합니다.
• 2단계: 결제 요청 API ( POST /api/v1/payments ) - 분리된 트랜잭션에서 PG사로 결제를 요청하고, 우리 DB에는 결제
이력을  READY  상태로만 남깁니다. PG와의 통신 중에는 DB 락이 없습니다.
• 선택지 B: 기존 단일 API 유지 (내부 복잡도 증가)
• 사용자 입장에서는 기존처럼 한 번의 API 호출만 하도록 둡니다. 대신 백엔드 내부에서 Spring Event나 메시지 큐
등을 활용하여 주문 생성 트랜잭션과 외부 결제 호출 트랜잭션을 강제로 쪼갭니다.
• 영향: 클라이언트 코드는 변하지 않지만 백엔드 구현 복잡도가 급격히 상승합니다.

### 2. 콜백 유실(누락) 보정 정책

[문제 상황 및 분석]
PG사의 결제 처리가 완료되면 우리 서버의 Callback API ( POST /api/v1/examples/callback  등)를 호출하여 최종
성공/실패 여부를 알려주게 됩니다. 하지만 PG사 오류나 일시적인 네트워크 장애로 인해 우리 서버가 이 콜백을 영영
수신하지 못하는 상황이 발생할 수 있습니다.
만약 콜백이 누락되면 우리 시스템의 데이터는 영원히  READY(결제대기)  /  PENDING(주문대기)  상태에 갇히게 되고,
선점된 재고도 복구되지 못한 채 묶여버립니다.

[선택지]

• 선택지 A (추천): 스케줄러(Batch)를 통한 자동 폴링 동기화
• 일정 시간(예: 3~5분)이 지나도 상태가  READY 인 결제 건들을 주기적으로 수집하는 백그라운드 스케줄러를
구현합니다.
• 스케줄러가 알아서 PG사의 "단건 결제 상태 확인 API ( GET /api/v1/payments/... )"를 호출하여 유실된 성공/실패
상태를 가져와 우리 DB에 자동 반영(재고 복구 등)합니다.
• 장점: 사용자의 개입 없이 시스템이 스스로 일관성을 보장하며 재고 고착을 막습니다.
• 선택지 B: 사용자/어드민 수동 트리거 위주 (자동화 없음)
• 스케줄러를 개발하지 않고, 사용자가 화면에서 "결제 상태 새로고침" 버튼을 누르거나 어드민이 백오피스에서
수동으로 동기화 버튼을 누를 때만 PG 상태를 확인합니다.
• 장점: 구현은 단순합니다.
• 단점: 사용자가 앱을 그냥 종료해버리거나 어드민이 확인하지 않으면 재고가 무한정 묶이는 치명적인 문제가
있습니다.
### 추가 선택지 C: 지연 메시지 큐(Delayed Queue)를 활용한 이벤트 기반 보정
[작동 방식]

### 💡 Redis TTL(만료 시간)을 활용한 보정 방식
1. 결제 요청 시 Redis에 5분짜리 임시 키 생성
   • 결제가  READY  상태로 접수되면, DB에 저장함과 동시에 Redis에  payment_check:12345  (결제ID) 라는 키를 만들고
   TTL(수명)을 5분으로 설정합니다.
2. 5분 뒤 Redis에서 키가 자동으로 삭제(만료)됨
   • 시간이 흘러 정확히 5분이 지나면 Redis 시스템이 해당 키를 지워버립니다.
3. Spring Boot가 "키 삭제 알림"을 수신 (Listener)
   • Redis는 키가 만료되어 삭제될 때 Spring Boot로 "방금  payment_check:12345  키가 만료되었습니다" 라고 알림을
   보내줄 수 있습니다. (Spring Data Redis의  KeyExpirationEventMessageListener  사용)
4. 보정 로직 실행
   • Spring Boot는 이 알림을 받아 결제 ID(12345)를 추출한 뒤, DB를 조회해봅니다.
   • 만약 그 사이 콜백이 와서 결제가  COMPLETED 로 확정되었다면 그냥 무시합니다.
   • 만약 여전히  READY  상태라면 (콜백이 유실되었다면), 그때 PG사의 결제 상태 조회 API를 호출하여 상태를 맞춥니다.

### 3. 결제 타임아웃(Read Timeout) 발생 시 처리 정책

[문제 상황 및 분석]
여기서 가장 주의할 점은, 우리가 타임아웃을 겪었다고 해서 PG사의 결제가 실패한 것은 아닐 수 있다는 점입니다. PG사는
우리 서버에서 PG사로 결제 요청( POST )을 보냈는데, 지정한 타임아웃 시간(예: 500ms) 안에 응답이 오지 않아 우리
서버에서 예외가 발생한 상황입니다.
여기서 가장 주의할 점은, 우리가 타임아웃을 겪었다고 해서 PG사의 결제가 실패한 것은 아닐 수 있다는 점입니다. PG사는
이미 사용자의 카드를 정상적으로 결제(승인)했는데 우리에게 응답을 주는 네트워크 과정에서만 지연이 생겼을 가능성이
큽니다.
이때 만약 우리가 타임아웃이 났다는 이유만으로 섣불리 주문을 취소해버리면, "고객의 돈은 빠져나갔는데 주문은 취소되어
버린" 최악의 결제 사고가 발생합니다.

[선택지]

• 선택지 A (추천): 최종 확정 지연 및 결과 확인 후 보상 트랜잭션 수행
• 타임아웃 에러가 떨어지더라도 절대 즉시 주문을 취소하거나 재고를 복구하지 않습니다. 주문을  PENDING(대기)
상태로 둡니다.
• 고객에게는 "결제 진행 상태를 확인 중입니다"라고 안내합니다.
• 이후 도착하는 PG사의 비동기 콜백이나 스케줄러(배치)를 통해 PG사가 해당 결제를 정말로 성공했는지, 아니면
접수조차 안된 실패인지 명확한 사실을 확인한 뒤에야 주문을  COMPLETED 로 확정하거나, 실패로 확인됐을 때만 보상
트랜잭션(재고 복구 등)을 실행하여  CANCELED  처리합니다.
• 선택지 B: 즉시 자체 취소 및 무조건 망취소(Cancel) 비동기 전송
• 타임아웃이 떨어지면 즉시 우리 서버의 주문을 취소하고 재고를 원상복구시킵니다.
• 동시에 PG사로 **"방금 보낸 요청이 성공했든 실패했든 무조건 취소(망취소) 해달라"**는 취소 API를 비동기로
던집니다.
• 고객에게는 "네트워크 문제로 취소되었습니다. 다시 시도해주세요."라고 안내합니다. (단, 망취소 통신마저 실패하면
환불이 붕 뜨게 되어 어드민의 수동 대사가 필요합니다.)

### 4. 중복 결제 및 이중 주문 생성 방지 (멱등성 보장)

[문제 상황 및 분석]
사용자가 결제 버튼을 빠르게 더블 클릭하거나, 모바일 앱에서 네트워크 지연으로 인해 자동으로 API를 재요청(Retry)하는
상황은 흔하게 발생합니다.
만약 동일한  orderId 로 결제 요청( /payments )이 동시에 2번 접수되면, PG사로도 요청이 2번 나가게 되어 고객의 돈이
이중으로 출금되는 치명적인 결함이 발생할 수 있습니다. 동일한 장바구니로 주문( /orders ) 자체가 2번 생성되는 것도
막아야 합니다.

[선택지]

• 선택지 A (추천): DB 제약 조건을 통한 원천 차단 + 멱등성 키 활용
• 결제 중복 방어: 가장 확실한 방법으로,  PAYMENTS  테이블의  order_id  컬럼에 **Unique Index (고유 인덱스)**를
설정합니다. 이렇게 하면 찰나의 동시 요청(Race condition)이 발생해도 DB 구조 자체에서 1주문 당 1결제
강제하므로 이중 결제가 물리적으로 불가능합니다. 요청 진입 시 주문 상태가  PENDING 인지도 함께 검증합니다.
• 주문 중복 방어: 클라이언트가 API를 호출할 때 헤더에 멱등성 키( Idempotency-Key , 예: UUID)를 실어 보내도록
하고, 서버는 이를 짧은 시간(예: 10분) 동안 Redis나 DB에 저장하여 같은 키로 들어오는 재요청은 무시하고 이전
결과를 그대로 반환합니다.
• 선택지 B: 애플리케이션 상태(Select) 검증으로만 방어
• DB에 Unique Index를 걸거나 클라이언트로부터 멱등성 키를 받지 않습니다.
• 오직 결제 요청이 들어올 때 애플리케이션 코드 레벨에서  SELECT  쿼리를 날려 "이 주문으로 진행 중인 결제가
있는지" 확인 후 막아냅니다.
• 단점: 0.001초 차이로 거의 동시에 요청이 들어올 경우 SELECT 쿼리를 동시에 통과해버려서 이중 결제가 발생할
위험이 있습니다. 이를 막으려면 추가적으로 테이블 락(비관적 락)을 걸어야 해서 병목이 생깁니다.

### 📝  /docs/week2  설계 문서 최종 변경 사항 요약

#### 1.  01-requirements.md  (요구사항 명세)
• [수정] 2.1 재고 관리 및 결제 정책
• 기존 "단일 트랜잭션 기반 주문/결제 처리" 문구를 삭제합니다.
• 변경: "주문 생성(재고 차감)과 결제 요청을 분리된 트랜잭션으로 처리한다. 결제 실패 혹은 타임아웃 발생 시 즉시
취소하지 않고, 비동기 콜백 혹은 Redis TTL 만료 이벤트를 통해 최종 상태를 확정하고 보상 트랜잭션(재고 복구)을
수행한다."
• [추가] 2.8 비동기 결제 및 멱등성 정책 (신규 항목)
• 중복 결제 방지를 위해 DB Unique 제약조건( order_id ) 및 멱등성 키 활용 정책 추가.
• 콜백 위조 방지를 위해 수신 즉시 PG사 상태 확인 API로 교차 검증(Cross-Validation) 수행 명시.
• [수정] 3.4 주문 API 리스트
• 기존:  POST /api/v1/orders/checkout  (단일 API)
• 변경:
•  POST /api/v1/orders  (주문 생성 및 재고 차감 ->  orderId  반환)
•  POST /api/v1/payments  (결제 요청 및 Redis TTL Key 생성)

#### 2.  02-sequence-diagrams.md  (시퀀스 다이어그램)

• [삭제] 기존의 거대한  주문 요청 API 시퀀스 다이어그램  삭제.
• [추가] 다음 3가지 독립된 시퀀스 다이어그램으로 대체됩니다.
1. 주문 생성 및 결제 요청 (Transaction 1, 2): 트랜잭션이 분리된 API 호출 흐름 및 PG 연동 중 DB 락 해방 과정.
2. 비동기 콜백 처리 (Transaction 3): 위조 방지 교차 검증(GET API) -> 결제/주문 상태 확정 -> 실패 시 보상
트랜잭션 흐름.
3. Redis TTL 만료에 의한 상태 복구: Redis  KeyExpiredEvent  발생 -> DB 조회 -> 누락된 콜백에 대해 PG사 상태
확인 API 찔러보기 -> 상태 확정 및 보상 트랜잭션 흐름.


#### 3.  03-class-diagram.md  (클래스 다이어그램)

• 도메인 클래스 변경:  Stock.restore() ,  CouponIssue.cancel()  등 보상 트랜잭션을 위한 롤백 메서드 책임 명확화.
(기존 메서드 활용)
• 파사드(Facade) 분리:  OrderFacade 에 집중되어 있던 로직을  OrderFacade.createOrder() 와  PaymentFacade.
processPayment() ,  PaymentFacade.handleCallback()  등으로 분리.
• 이벤트 리스너 추가: Redis 키 만료 이벤트를 수신하는  PaymentExpirationListener  (Application 계층) 추가.

#### 4.  04-erd.md  (ERD)

• PAYMENTS 테이블 인덱스 추가:
• 동시성 이슈 원천 차단을 위해  order_id  컬럼에  Unique Index  제약조건 추가 명시.
• (그 외 주문, 결제 상태 값인  PENDING ,  READY ,  COMPLETED  등은 기존 구조를 그대로 활용합니다.)


---


### 1️⃣ 기능이 아니라 "불확실성" 관점의 재해석

현재 설계는 외부 PG 시스템의 동작을 전혀 신뢰하지 않는다는 훌륭한 전제를 깔고 있습니다.
• 지연될 수 있다: PG 호출(1~5초)을 DB 트랜잭션과 분리하여, 지연이 발생해도 내부 시스템의 커넥션 고갈을 막고
있습니다.
• 실패할 수 있다 (타임아웃): 타임아웃 발생 시 섣불리 실패 처리하지 않고 상태를  READY 로 대기시켜, 실제 PG의 성공
여부와 시스템 상태가 어긋나는 것을 방어하고 있습니다.
• 중복 실행될 수 있다:  PAYMENTS  테이블의  order_id  Unique Index와 멱등성 키를 통해 방어하고 있습니다.
• 성공했지만 응답이 유실될 수 있다: 비동기 콜백 누락을 대비해 Redis TTL( KeyExpirationEvent )을 안전망으로 두고
있습니다.
──────
### 2️⃣ 트랜잭션 경계 검증

현재 설계의 트랜잭션 경계는 3개(주문 생성 / 결제 요청 / 콜백-보상)로 잘게 쪼개져 있습니다. 외부 호출은 트랜잭션
외부에 존재하여 아키텍처적으로 안전합니다.

• 외부 호출 실패 시 내부 상태는 어떻게 되는가?
• 타임아웃 등 에러 발생 시 시스템 상태는  PENDING(주문) / READY(결제) 를 유지합니다. 즉시 취소되지 않습니다.
• 내부 커밋 이후 외부 호출 실패 시 복구 가능한가?
• 내부 커밋(주문 생성) 이후 결제 자체가 아예 안 날아간 경우, Redis TTL 만료 이벤트가 5분 뒤 트리거되어 단건
조회를 해보고 내역이 없으면 보상 트랜잭션(재고 복구)을 실행하므로 복구 가능합니다.
• 외부 성공 후 내부 실패 시 상태는 어떻게 정합성을 유지하는가?
• PG는 성공해서 콜백을 줬는데, 우리 서버 내부에서 DB 장애 등으로 콜백 처리 트랜잭션이 롤백될 수 있습니다. 이
경우 상태가 일치하지 않게 됩니다.
──────
### 3️⃣ 상태 기반 구조 전이

호출 흐름을 걷어내고 순수하게 **상태(State)**의 전이만 놓고 보겠습니다.
• [내부 상태]  PENDING / READY  ───(대기 구간)───▶  COMPLETED / APPROVED  (성공 시)
• [외부 상태] ───────────(승인 처리)───────────▶  SUCCESS
• ⚠️ 상태가 어긋나는 지점 (Risk Point):
• 대기 구간( READY  상태)에서는 내부 시스템은 결제가 완료되었는지 절대 알 수 없습니다.
• 이 상태에서 클라이언트(사용자)가 탭을 닫아버리거나 재시도를 누를 때의 UX 처리가 매우 중요해집니다.
──────
### 4️⃣ 중복 요청 및 재시도 가능성 분석
네트워크 지연으로 클라이언트가 결제 버튼을 두 번 눌렀다고 가정해보겠습니다.
• 현재 설계의 방어:  PAYMENTS.order_id 에 Unique Index가 걸려 있으므로, 2번째 요청은 DB Insert 과정에서
DataIntegrityViolationException 을 발생시켜 완벽하게 이중 결제를 차단합니다.
• 잠재적 문제 (동시성 멱등성):
만약 "PG사 콜백 수신"과 "Redis TTL 만료 이벤트"가 1ms 차이로 동시에 발생한다면 어떻게 될까요?
두 개의 스레드가 동시에  READY  상태를 읽고, 각각 DB 업데이트 및 확정 처리를 시도하는 Race Condition이 발생할 수
있습니다.
──────
### 5️⃣ 장애 시나리오 분석

정상 흐름은 작동할 것입니다. 하지만 다음과 같은 실패 흐름(장애 시나리오)에 직면했을 때 시스템의 반응을
살펴보겠습니다.
1. 시나리오 A: 보상 트랜잭션 처리 중 내부 DB 장애 (매우 치명적)
   • 상황: PG사 잔액 부족으로 결제 실패 콜백을 받았습니다. 시스템이 재고를 복구(보상 트랜잭션)하려는데 DB
   데드락이나 장애로 인해 복구가 실패(Rollback)합니다.
   • 결과: 결제는 실패했는데, 재고는 영구히 차감된 상태로 남게 됩니다 (정합성 파괴).
   • 복구 가능성: 현재 아키텍처에서는 보상 트랜잭션 실패에 대한 '재시도(Retry)' 로직이 보이지 않아 수동 복구가
   필요합니다.
2. 시나리오 B: Redis 시스템의 일시적 다운
   • 상황: 결제는 들어왔지만 콜백이 유실되었는데, 마침 캐시용으로 쓰던 Redis 서버가 잠시 다운되거나
   재시작되었습니다.
   • 결과: 5분 TTL 알람( KeyExpiredEvent )이 영구히 소실됩니다.
   • 복구 가능성: 알람 시계가 고장 났으므로 해당 결제 건은 영원히  READY  상태에 갇히게 됩니다.
3. 시나리오 C: 콜백 상태 확정 중 동시성 충돌
   • 상황: 관리자가 수동으로 결제 상태 확인 버튼을 누르는 순간과, 비동기 콜백이 들어오는 순간이 겹칩니다.
   • 결과: 주문 상태 업데이트 과정에서 낙관적 락/비관적 락 처리가 없다면 덮어쓰기(Lost Update)가 발생할 수
   있습니다.

──────
### 6 잠재 리스크 및 대안 구조 선택지

현재 설계는 "외부 시스템 지연 격리"라는 목적은 훌륭하게 달성했지만, 보상 트랜잭션 실패나 보조 시스템(Redis) 유실 시
데이터가 붕 뜨는 리스크를 내포하고 있습니다. 이를 보완하기 위한 대안 설계 선택지입니다.

• 선택지 1: 보상 로직의 단순화 유지 (현재 상태)
• 리스크를 수용합니다. 보상 트랜잭션이 실패하거나 Redis가 다운되는 일은 극히 드물다고 가정하고, 에러 로그에
알럿(Slack 등)을 걸어 어드민이 수동 대사(Manual Reconciliation)로 풀어냅니다.
• 영향도: 개발 복잡도가 가장 낮고 실용적입니다.
• 선택지 2: Redis 알람 + DB 스케줄러 이중화
• Redis TTL 이벤트를 주력으로 쓰되, 새벽 시간에 한 번씩 도는 DB 스케줄러(예: 2시간 이상 READY인 건 색출)를
최후의 안전망으로 하나 더 둡니다.
• 영향도: Redis 이벤트 유실이라는 치명적 단점을 가볍게 커버할 수 있는 훌륭한 타협안입니다.
• 선택지 3: 비동기 메시지 큐 기반의 완벽한 재시도 (Outbox Pattern)
• 보상 트랜잭션 요청 자체를  Kafka 나  RabbitMQ 의 실패 재시도 큐에 담아, 성공할 때까지 무한 재시도하게
만듭니다.
• 영향도: 데이터 정합성을 기계적으로 보장할 수 있으나, 시스템 인프라 및 운영 복잡도가 기하급수적으로 상승합니다.


현재 설계의 경계와 상태 분리는 매우 우수합니다. 발생 가능한 장애 시나리오(특히 보상 트랜잭션 실패)를 염두에 두시고,
비즈니스 요건에 맞�춰 리스크 수용 수�준을 결하시면 더욱 견고�한 시스템이 될 것�입니다.

두 방식을 비교해보면 무조건 "전자가 더 좋다"라고 말하기는 어렵습니다. 서비스의 트래픽 규모와 아키텍처의 복잡도를
어디까지 허용할 것인가에 따라 정답이 달라집니다.
실무적인 관점에서 두 방식의 장단점을 명확히 비교해 드리겠습니다.
──────
### 1️⃣ RDB 스케줄링만 사용하는 방식 (단일화)
DB에 일정 시간(예: 5분) 이상  READY  상태로 머물러 있는 결제 건을  SELECT  해서 보정하는 단순한 방식입니다.
• 👍 장점 (Simplicity & Reliability):
• 압도적인 단순함: 백그라운드 스케줄러( @Scheduled ) 하나만 있으면 됩니다. 아키텍처가 단순해서 유지보수와
디버깅이 매우 쉽습니다.
• 절대 유실되지 않음: DB 자체가 Source of Truth(단일 진실 공급원)이므로, 서버가 꺼졌다 켜져도 데이터만
남아있으면 언젠가는 무조건 복구해 냅니다.
• 👎 단점:
• DB 부하 (Polling Overhead): 1분마다  SELECT * FROM payments WHERE status = 'READY'  쿼리를 날려야 합니다.
인덱스( status, created_at )를 잘 태우면 큰 문제는 없지만, 트래픽이 엄청나게 많은 대용량 서비스에서는 이
주기적인 폴링 자체가 DB에 지속적인 피로도를 줍니다.


### 2️⃣ Redis 만료 이벤트 + RDB 스케줄링 (이중화)

Redis TTL을 주력으로 쓰되, 혹시 모를 Redis 유실(Pub/Sub은 휘발성)을 대비해 새벽 시간에 RDB 스케줄러를 한 번씩
돌리는 방식입니다.

• 👍 장점 (Performance):
• DB 피로도 제로: DB를 주기적으로 긁지 않고, 정확히 5분이 지난 결제 건에 대해서만 알람(이벤트)이 울려
핀포인트로 처리하므로 성능상 매우 우아합니다.
• 👎 단점 (Complexity):
• 관리 포인트 증가: "결제 복구"라는 하나의 목적을 위해 Redis Listener 로직과 RDB Scheduler 로직 두 가지를 모두
유지보수해야 합니다.
• Redis Pub/Sub은 본질적으로 "Fire and Forget(쏘고 잊기)" 방식이라 알람이 울릴 때 서버가 배포 중이면 이벤트가
영구 증발합니다. 그래서 결국 RDB 스케줄러를 또 만들어야 하는 모순이 생깁니다.
──────
### 💡 결론 및 실무 추천

• 스타트업 ~ 중대형 서비스: RDB 스케줄링만 사용하는 것을 강력히 추천합니다.
대부분의 서비스 규모에서는 1분에 한 번씩 인덱스를 타는 상태 체크 쿼리를 날리는 것이 DB에 전혀 무리가 되지 않습니다.
오히려 관리 포인트를 하나로 줄여 시스템 복잡도를 낮추는 것이 훨씬 이득입니다.
• 초대형 플랫폼 (네이버페이, 배달의민족 급): Redis(또는 Kafka) + RDB 스케줄러를 혼합합니다.
분당 수만 건이 쏟아져 DB 폴링 자체가 병목이 되는 수준이 오면, 이벤트 기반으로 부하를 분산하고 스케줄러는 하루에 한
번만 돌려 최후의 정산 용도로만 사용합니다

---

redis ttl 없이 스케줄러로만 콜백 누락을 커버하는 경우, 일정 시간마다 실행되는 스케줄링이 트래픽에 영향이 별로 없다는
사실을 어떤 식으로 테스트해서 알 수 있어?
▸ Thought for 4s
Prioritizing Tool Usage
Redis TTL 대신 스케줄러(Cron 등)를 주기적으로 실행하여 DB를 폴링(Polling)하는 방식으로 콜백 누락을 처리할 때, 이
스케줄러가 일반 유저 트래픽이나 DB 성능에 미치는 영향을 검증하려면 **'부하 테스트(Load Testing)'**와 **'DB
모니터링'**을 결합하여 증명해야 합니다.

이 사실을 증명하기 위한 구체적인 테스트 시나리오와 방법을 단계별로 정리해 드립니다.
──────
### 1. 테스트 환경 및 더미 데이터 준비 (Data Preparation)
스케줄러가 아무 데이터도 없을 때 도는 것은 의미가 없습니다. 실제 트래픽 부하 상황을 모사해야 합니다.

• 일반 데이터 세팅: 수만~수십만 건의 일반 상품/주문/유저 데이터를 DB에 밀어 넣습니다.
• 스케줄러 타겟 데이터 세팅: 결제가 누락되어 스케줄러가 실제로 조회하고 처리해야 할  상태=READY  이면서  생성된 지
5분 경과 된 결제 레코드를 수백~수천 건 고의로 만들어 둡니다.
### 2. 베이스라인(Baseline) 성능 측정 (스케줄러 OFF)

스케줄러를 끈 상태에서 평시 트래픽 상황을 측정하여 기준점(Baseline)을 잡습니다.
• 부하 발생 도구 사용:  k6 ,  JMeter ,  nGrinder ,  Locust  같은 툴을 사용하여 가상의 유저 시나리오(상품 조회 ->
주문 -> 결제 요청) 스크립트를 작성합니다.
• 부하 발생: 목표 트래픽(예: VUser 500명, 1000 TPS)을 시스템에 지속적으로 발생시킵니다.
• 지표 기록:
• API 응답 속도 (Latency - 평균, p95, p99)
• 초당 처리량 (TPS / RPS)
• 웹 애플리케이션 및 DB 서버의 CPU / Memory 사용률
• DB 커넥션 풀(HikariCP 등) 사용량

### 3. 스케줄러 동시 실행 부하 테스트 (스케줄러 ON)
베이스라인 측정이 끝났다면, 이제 스케줄러를 켠 상태에서 완전히 동일한 부하를 주입합니다.
• 스케줄러 동작: 스케줄러를 1분(혹은 설정한 주기)마다 동작하게 켭니다. 스케줄러가 대량의  READY  상태 레코드를 한
번에 읽어와 PG사에 상태 조회를 요청하고 DB를 업데이트하도록 둡니다.
• 동일 부하 발생: 앞서 작성한 부하 테스트 스크립트를 스케줄러가 도는 도중에 동시에 실행합니다.
### 4. 영향도 분석 및 결과 비교 (어떤 지표를 봐야 하는가?)

이 테스트를 통해 다음 항목들을 비교 분석함으로써 "영향이 없다(혹은 미미하다)"는 것을 증명할 수 있습니다.

#### A. 애플리케이션 수준의 영향
• TPS 및 Latency 비교: 스케줄러 ON 상태의 메인 API들(특히 상품 조회나 주문 생성)의 p95(상위 95%) 응답 속도가
스케줄러 OFF 시점과 비교했을 때 거의 차이가 없다면, 웹 요청을 처리하는 쓰레드가 스케줄러에 의해 블로킹되지 않음을
증명하는 것입니다.
• 쓰레드 풀 분리 확인: 모니터링 툴(APM, Actuator 등)을 통해 톰캣 워커 쓰레드( http-nio... )와 스케줄러 쓰레드(
scheduling-1... )가 자원 경합을 벌이지 않는지 확인합니다.

#### B. 데이터베이스 수준의 영향 (가장 중요)

스케줄러의 가장 큰 리스크는 **DB I/O 및 락(Lock)**입니다.

• 커넥션 점유율: 스케줄러가 너무 많은 데이터( List<Payment> )를 한 번에 퍼올리느라 DB 커넥션을 오래 물고 있으면
일반 주문/결제 요청이 DB 커넥션을 얻지 못해 대기(Timeout)가 발생합니다. (HikariCP Active Connections 모니터링)
• 슬로우 쿼리(Slow Query) 분석: 스케줄러가 실행하는  SELECT * FROM payment WHERE status = 'READY' AND created_at <
?  쿼리가 Index를 제대로 타는지, Full Table Scan을 발생시켜 DB CPU를 치솟게 하지 않는지 DB 실행 계획(EXPLAIN)과
슬로우 쿼리 로그를 통해 증명합니다.
• 테이블/레코드 락 대기: 스케줄러가 상태를  FAILED/APPROVED 로  UPDATE  할 때 트랜잭션 범위가 너무 커서 테이블
락이나 불필요한 데드락을 유발하지 않는지 확인합니다.
──────
### 💡 결론 및 방어 논리 도출

위의 테스트를 거친 후, 결과를 바탕으로 다음과 같이 아키텍처적 타당성을 설명할 수 있습니다.

│ *"k6를 이용해 초당 500건의 메인 API 트래픽을 발생시키는 도중, 1분 주기의 보정 스케줄러가 1,000건의 누락 데이터를
│ DB에서 폴링하여 업데이트하는 테스트를 진행했습니다.
│
│ 테스트 결과, 해당 컬럼에 적절한 인덱스를 태워 DB 조회 성능 병목이 없었으며, 스케줄러 로직을 별도의 쓰레드풀과
│ 청크(Chunk) 단위 트랜잭션으로 분리 처리하여 메인 주문 API의 p95 응답 속도 저하율을 2% 미만으로 억제했습니다.
│
│ 따라서 현재 트래픽 규모에서는 관리 포인트가 늘어나는 Redis TTL 이벤트 리스너 방식보다, 단순한 스케줄링 폴링
방식이
│ 시스템 안정성이나 성능 면에서 무리가 없음을 확인했습니다."*

이렇게 **'동일 부하 상황에서의 메인 API 응답 지연율 변화'**와 'DB 쿼리 성능(인덱스 확인)' 두 가지 지표를 들이미는
것이 가장 확실한 증명 방식입니다.

### 1️⃣ 문제 상황으로 재설명 (Why & What)
단순히 "timeout시 retry하고, ttl 만료 시 실패 처리한다"가 아니라, 현재 어떤 문제가 있고 왜 해결해야 하는지 관점을
나누어 정리합니다.
• 사용자 관점: 결제를 진행했는데 "결제 중" 화면에서 멈추거나 결과를 하염없이 기다리는 문제를 해결하려는 것입니다.
문제가 생기면 빠르게 실패 처리됨을 인지하고 다시 구매(재시도)할 수 있어야 합니다.
• 비즈니스 관점: 외부 결제 응답이 지연되거나 유실되면 해당 상품의 재고가 '가선점'된 상태로 영원히 묶여 다른 고객의
구매를 방해하게 됩니다. 따라서 일정 시간이 지나면 가선점을 해제(롤백)하여 재고 정합성을 맞춰야 합니다.
• 시스템 관점: 외부 PG사의 네트워크 단절, 지연 등으로 콜백(비동기 응답)이 오지 않을 때, 시스템이 이를 수동적으로
기다리지 않고 능동적으로 상태를 확인(Retry)하거나 자가 복구(TTL 만료를 통한 강제 실패 처리)하는 메커니즘이
필요합니다.
──────
### 2️⃣ 애매한 요구사항 드러내기 (결정 필요한 부분)
요구사항 중 명확히 결정되지 않은 부분들을 드러냅니다.
1. 정책 질문: "Retry"의 정확한 의미와 대상은 무엇인가요?
   • PG사로부터 콜백이 오지 않을 때, 우리 서버가 PG사 API를 찔러 결제 상태를 직접 다시 물어보는(조회하는) 행위를
   의미하나요?
   • 만약 그렇다면, 결제 요청 후 언제(예: 3초 후, 10초 후?) 상태를 재확인할 것인지 기준 시간이 필요합니다.
2. 경계 질문: "유저에게 다시 시도하게 하는 방식"은 어떻게 전달되나요?
   • 시스템이 실패 처리(재고 롤백 등)를 완료했을 때, 사용자에게 이를 어떻게 알릴 것인가의 책임 문제입니다. 알림
   시스템(푸시/알림톡)을 연동해야 하는지, 아니면 사용자가 주문 내역(Front-end)을 새로고침했을 때 상태만 '실패'로
   보여주면 되는지 명확히 해야 합니다.
3. 정책 질문: 쿠폰 복구 정책
   • 결제 실패로 인한 보상 트랜잭션 시, 재고뿐만 아니라 사용한 쿠폰도 다시 사용 가능한 상태로 롤백해야 하는지
   확인이 필요합니다.
   ──────
### 3️⃣ 옵션 및 영향도 제시 (질문)
개발 및 설계 방향을 정하기 위해 다음 옵션 중 하나를 선택해 주세요.
Q1. 콜백 Timeout 시 Retry (PG사 상태 확인) 주체 및 시점
• 선택지 A: 단기 TTL 방식 (실시간성 높음)
• 결제 요청 시 짧은 TTL(예: 10초)을 걸어두고, 만료 시점에 워커가 PG사 상태 조회 API를 1~3회 호출하여 결제
결과를 확정.
• 영향도: 구조가 조금 복잡해지지만, 빠른 시간 내에 상태를 확정할 수 있음.
• 선택지 B: 스케줄러 기반 폴링 방식 (구현 단순)
• 주기적(예: 1분마다)으로 배치 스케줄러가  PENDING  상태인 결제건을 모아 PG사에 상태를 재확인.
• 영향도: 구현이 매우 단순하지만, 사용자 입장에서 실패/성공을 인지하기까지 최대 1분이 걸릴 수 있음.
Q2. 콜백 완전 누락 시 최종 실패 처리(TTL) 및 재시도 유도 방식
• 선택지 A: Long TTL(예: 5분) 이벤트 기반의 자동 보상 트랜잭션
• 최종적으로 콜백도 오지 않고 Retry도 실패한 경우, 5분짜리 긴 TTL 만료 이벤트를 통해 결제를  FAILED 로 변경하고
재고/쿠폰을 롤백. 유저에게는 앱 내 푸시나 알림톡 발송하여 재주문 유도.
• 영향도: 사용자 경험은 매우 좋으나 알림 발송 인프라 연동 필요.
• 선택지 B: 단순 상태 변경 및 Front-end 의존
• TTL 만료 시 상태만  FAILED 로 변경 및 재고/쿠폰 롤백. 유저는 화면을 새로고침 하거나 주문 내역에서 '시간
초과로 인한 취소'를 확인하고 직접 다시 주문.
• 영향도: 구현 및 관리가 가장 쉬움. (1주 차 요구사항 수준에 적합할 수 있음)
──────
### 4️⃣ 개념 모델 (구현 전 사고 정렬)

합의를 돕기 위해 현재 이해한 바탕으로 개념을 정의합니다.
• 액터: 결제를 진행하는 사용자 (정상적인 결제 흐름을 기대함)
• 핵심 도메인:
•  Payment : 결제 상태( PENDING ,  SUCCESS ,  FAILED )를 관리.
•  Order  &  Product : 보상 트랜잭션 발생 시 재고 가선점 롤백 대상.
• 보조/외부 시스템:
•  PG (Payment Gateway) : 실제 결제를 처리하고 콜백을 쏴주며, 상태 재확인(Retry) API를 제공함.
•  Redis : 짧은 시간/긴 시간의 TTL 만료 이벤트를 발행하여 우리 시스템을 깨워주는(Trigger) 타이머 역할.
──────
### 5️⃣ 흐름 다이어그램 (Sequence Diagram)
이 다이어그램은 결제 요청 후 비동기 응답(콜백) 처리가 지연될 때, 시스템이 어떻게 개입하여 상태를 확정하고 보상
트랜잭션을 일으키는지 그 책임의 흐름을 검증하기 위해 작성되었습니다. (Q1 옵션A, Q2 옵션B를 가정한 초안입니다)
sequenceDiagram
actor User as 사용자
participant App as Application Layer (Facade)
participant Redis as Redis (TTL Events)
participant PG as PG 시스템
participant Domain as Domain Layer (Payment, Order)

        User->>App: 1. 결제 요청 (Idempotency Key)
        App->>Domain: 2. 결제 상태 PENDING 처리
        App->>Redis: 3. 단기 TTL (10초, Retry용) 설정
        App->>Redis: 4. 장기 TTL (5분, 완전 실패용) 설정
        App-->>User: 5. 결제 진행 중 안내

        %% 케이스 1: 단기 TTL 만료 (콜백 지연/Timeout)
        Note over App, Redis: 10초 경과
        Redis-->>App: 6. 단기 TTL 만료 이벤트 발생
        App->>PG: 7. 상태 조회 API 호출 (Retry)

        alt PG 결제 성공 상태 리턴
            PG-->>App: 8a. 성공 응답
            App->>Domain: 9a. 결제 SUCCESS 확정 및 장기 TTL 해제
        else PG 결제 실패/미결제 상태 리턴
            PG-->>App: 8b. 실패 응답
            Note right of App: 계속 기다림 (장기 TTL 만료 대기)
        end

        %% 케이스 2: 장기 TTL 만료 (콜백 완전 누락)
        Note over App, Redis: 5분 경과
        Redis-->>App: 10. 장기 TTL 만료 이벤트 발생
        App->>Domain: 11. 결제 FAILED 확정
        App->>Domain: 12. 보상 트랜잭션 (재고/쿠폰 롤백)
        App-->>User: 13. 주문 내역에서 '결제 실패' 노출 (재시도 유도)

다이어그램 읽는 법:

• 이 구조에서 특히 봐야 할 포인트는 Redis를 통한 두 개의 타이머(단기 Retry, 장기 FAILED) 설정입니다.
• 앱은 PG 콜백을 수동적으로 기다리기만 하지 않고, 단기 TTL 만료 시점에 직접 PG사에 상태를 물어보아(Retry) 불확실한
상태를 해소합니다.
──────
### 7 설계의 잠재 리스크

• Race Condition (동시성 문제):
정상적인  PG사 콜백  도착 시점과 Redis의  TTL 만료 이벤트 가 0.01초 차이로 거의 동시에 애플리케이션으로 들어올 수
있습니다. 이 경우 하나는 결제를 성공시키고, 다른 하나는 취소(재고 복구)를 시도하여 데이터 정합성이 크게 깨질 수
있습니다.
• 해결 선택지:
1. DB의  PAYMENTS  테이블 조회 시 **비관적 락(Pessimistic Lock)**을 걸어 먼저 도착한 요청이 상태를 변경하면
뒤늦은 요청은 무시(멱등성 보장)하도록 처리.
2. 분산 락(Redisson)을 사용하여 주문 번호(OrderId) 단위로 동시 접근을 제어.


──────
개발자님, 위의 Q1, Q2 옵션 및 쿠폰 복구 정책에 대해 어떻게 진행할지 결정해주시면, 이를 바탕으로  /docs/week2 의
요구사항, 시퀀스 다이어그램, ERD 등을 수정하겠습니다!

네, 알겠습니다. 첫 번째 질문에 대해 각 방식이 시스템과 사용자 경험에 미치는 영향을 자세히 설명해 드리겠습니다.
──────
### Q1. 콜백 Timeout 시 Retry (PG사 상태 재확인) 방식 선택
결제 요청 후, PG사에서 보내주는 비동기 콜백(성공/실패 응답)이 지연되거나 오지 않을 때 우리 서버가 PG사 측에 "이
결제건 정말 성공했어?"라고 직접 찔러보는(Retry) 방식을 결정해야 합니다. 다음 두 가지 옵션 중 하나를 선택해 주세요.
#### 선택지 A: 단기 TTL 기반 이벤트 방식 (Event-Driven)

• 작동 방식: 결제 요청을 보낼 때 Redis에 만료 시간이 짧은(예: 10초~30초) 키를 생성합니다. 10초 안에 콜백이 안 오면
Redis에서 만료 이벤트( KeyExpirationEvent )가 발생하고, 서버가 이 이벤트를 감지하여 즉시 PG사의 결제 상태 조회
API를 호출합니다.
• 장점 (사용자 경험/실시간성):
• 문제가 발생했을 때 거의 실시간(지정한 초 단위)으로 대응할 수 있습니다.
• 사용자가 결제 화면에서 무의미하게 대기하는 시간을 최소화할 수 있습니다.
• 단점 (복잡도/리스크):
• Redis의 만료 이벤트 수신용 워커(Listener)를 추가로 구현해야 합니다.
• 짧은 시간 내에 대량의 결제가 타임아웃될 경우, 순간적으로 워커에 부하가 몰릴 수 있습니다.


#### 선택지 B: 배치 스케줄러를 통한 폴링 방식 (Polling)
• 작동 방식: Redis의 만료 이벤트에 의존하지 않고, 스프링 배치의  @Scheduled  등을 사용해 주기적(예: 1분마다)으로
DB를 뒤져봅니다. 상태가 아직  PENDING 인 채로 일정 시간(예: 1분)이 지난 주문들을 모아서 일괄적으로 PG사에 상태를
조회합니다.
• 장점 (안정성/단순함):
• 구현이 매우 단순하며 직관적입니다.
• 일정 주기로 모아서 처리하므로 시스템의 순간적인 부하(Spike)를 방지할 수 있고 처리가 안정적입니다.
• 단점 (지연 발생):
• 스케줄러 주기에 따라 사용자는 최대 1분 이상 자신의 결제가 성공했는지 실패했는지 모른 채 기다려야 할 수
있습니다. 실시간성이 다소 떨어집니다.

──────
어느 방향이 현재 서비스의 방향성과 더 맞다고 생각하시나요? 편하신 방식으로 답변해 주시면, 이어서 두 번째 질문(콜백
완전 누락 시 최종 실패 처리 방식)을 상세히 드리겠습니다.

────────────────────────────────────────────────────────────
> pg-simulator는 지연되는 경우 5초까지 지연된다고 되어있는 것 같은데 그러면 ttl을 선택하는 경우엔 5초로 하면 되는걸까?

▸ Thought for 1s, 552 tokens
Prioritizing Tool Usage
좋은 질문입니다! PG 시뮬레이터가 최대 5초까지 지연될 수 있다면, TTL을 정확히 5초로 설정하는 것은 약간의 리스크가
있습니다.
그 이유는 동시성(Race Condition) 문제 때문입니다.
PG사 콜백이 정확히 5초 만에 도착했는데, 우리 서버의 Redis TTL 5초 만료 이벤트도 동일한 시점에 발생한다면 어떻게
될까요? 콜백을 처리하는 스레드와 만료 이벤트를 처리(Retry)하는 스레드가 동시에 결제 데이터를 건드리게 되어 시스템에
혼란을 줄 수 있습니다.

따라서 단기 TTL 방식을 선택하신다면 다음과 같이 설정하는 것을 권장합니다.
• 권장 단기 TTL: 10초 (또는 최소 7~8초)
• 이유: PG사가 보장하는 최대 지연 시간(5초)을 온전히 기다려준 뒤, 네트워크 지연을 고려하여 약간의 버퍼(+2~5초)를
둡니다. 이 시간 안에도 오지 않는다면 확실히 문제가 생겼다고 판단(Timeout)하고 10초 시점에 PG사에 상태를
재확인(Retry)하는 것이 가장 안전합니다.
설명해 드린 리스크(동시성 제어 필요)를 감안하더라도, 실시간성이 좋은 **선택지 A (단기 TTL 방식, 10초 설정)**로
결정을 진행할까요? 아니면 여전히 고민되시는 부분이 있을까요?

────────────────────────────────────────────────────────────
> redis 자체에 문제가 생겨서 제대로 처리하지 못하는 경우도 있을 텐데, 그런 경우를 대비해 하루 특정 시간마다
스케줄링으로 보정하는 게 좋을까?

▸ Thought for 3s
Prioritizing Tool Usage
네, 정확히 짚어주셨습니다! 개발자님께서 제안해주신 방식이 실제 실무의 결제/주문 시스템에서 가장 권장되는 '이중
안전장치(Belt and Suspenders)' 아키텍처입니다.

Redis 단기 TTL에만 전적으로 의존하면, Redis 서버가 다운되거나 메모리 문제로 이벤트가 유실되었을 때 데이터가 영원히
PENDING  상태로 남는 치명적인 버그(좀비 데이터)가 발생합니다.
따라서 다음과 같이 조합하는 것이 가장 이상적인 설계입니다.
1. 1차 방어 (빠른 고객 경험): Redis 단기 TTL (10초)
   • 대부분의 지연 케이스를 10초 만에 캐치하여 PG사 상태를 재확인하고 빠르게 상태를 확정해 줍니다.
2. 최후의 보루 (데이터 정합성): 주기적 스케줄링 (Fallback Batch)
   • 하루 1번(새벽 시간) 혹은 1시간마다 스케줄러를 돌립니다.
   • Redis 장애로 인해 TTL 이벤트를 타지 못하고 DB에 오랜 시간  PENDING 으로 남아있는 결제건들을 찾아내 일괄적으로
   PG사에 조회하거나 실패/취소(보상 트랜잭션) 처리하여 시스템 정합성을 맞춥니다.
   이렇게 단기 TTL(10초) + 백업 스케줄러 구조로 결합하여 문서에 반영하도록 하겠습니다. 훌륭한 설계 방향입니다!
   ──────
   이제 **두 번째 질문(콜백 누락 시 최종 실패 처리 및 재시도 유도 방식)**에 대해 상세히 설명해 드리겠습니다.

### Q2. 콜백 완전 누락 시 '최종 실패 처리(장기 TTL)' 및 '유저 재시도 유도' 방식
10초 시점에 PG사에 Retry를 해봤는데도 PG사 장애 등으로 여전히 "미결제" 상태이고, 이후로도 콜백이 영영 오지 않는
최악의 상황입니다. 이 결제 건을 영원히 기다릴 수는 없으므로 특정 시간(예: 5분)이 지나면 강제로 실패( FAILED )
처리하고 가선점된 재고를 롤백해야 합니다.

이때 사용자에게 "다시 시도해달라"고 유도하는 방식을 결정해야 합니다.

#### 선택지 A: 시스템 능동 알림 방식 (푸시/알림톡 등)

• 작동 방식: 5분 장기 TTL이 만료되면 서버가 결제를 강제 취소/재고 롤백합니다. 동시에 사용자에게 "결제 시간이
초과되어 취소되었습니다. 다시 주문해 주세요."라는 앱 푸시 알림이나 이메일(혹은 콘솔 로그 출력 등으로 갈음)을
발송합니다.
• 장점: 사용자가 앱을 끄고 나갔더라도 상황을 인지하고 다시 돌아올 확률이 높습니다.
• 단점: 알림 발송 시스템 연동 등 추가 구현 비용이 듭니다.
#### 선택지 B: 수동 확인 및 Front-end 의존 방식
• 작동 방식: 5분 장기 TTL 만료 시 DB 상태만 조용히  FAILED 로 변경하고 재고를 롤백합니다. 사용자가 주문 내역
페이지를 새로고침해서 보거나, 장바구니로 돌아갔을 때 UI 상에 "이전 결제가 시간 초과로 실패했습니다. 다시
결제해주세요."라는 문구를 노출하여 자연스럽게 재시도를 유도합니다.
• 장점: 백엔드는 상태값 변경만 확실히 책임지면 되므로 구현이 매우 직관적이고 단순합니다.
• 단점: 사용자가 직접 화면을 새로고침하거나 내역에 들어가지 않으면 실패 여부를 바로 알기 어렵습니다.
현재 프로젝트의 복잡도를 고려했을 때, 어떤 방식으로 유저의 재시도를 유도하는 것이 적합할까요? (만약 B를
선택하신다면, 백엔드에서는 별도의 알림 발송 로직 없이 상태 업데이트 및 롤백까지만 구현하는 것으로 문서를
정리하겠습니다.)

────────────────────────────────────────────────────────────
> 아직 첫번째 질문에 대해 더 얘기해야해. retry 횟수는 3번으로 하고 각 retry마다 최대 10초를 기다리는 걸로 반영하면 어떤
구조야?

▸ Thought for 4s
Prioritizing Tool Usage
앗, 제가 너무 성급하게 넘어갔군요! 죄송합니다. 첫 번째 질문에 대해 개발자님이 제안하신 "재시도 3회, 각 재시도 간격
10초 대기" 구조를 적용했을 때 시스템이 어떻게 돌아가는지 자세히 설명해 드리겠습니다.
해당 정책을 반영하면, 백그라운드 스레드를 오랫동안 점유하여 발생하는 성능 저하(Thread Blocking)를 막기 위해 보통
'연쇄적인 Redis TTL (Chained TTL)' 구조를 사용합니다.
### 🔄 구조 설명: "최대 3회, 10초 간격 Retry" 아키텍처
1. 최초 요청 (0초):
   • 사용자가 결제를 요청하면 상태를  PENDING 으로 두고, Redis에  payment:retry:{orderId}  키를 생성합니다.
   • 이 키의 값에는 **현재 재시도 횟수(0)**를 저장하고, TTL을 10초로 설정합니다.
2. 1차 재시도 (10초 경과):
   • 10초 후 Redis 만료 이벤트가 발생합니다.
   • 워커가 이벤트를 수신하여 PG사에 상태를 조회(1차 Retry)합니다.
   • 결과가 미결제/응답 지연일 경우: 재시도 횟수를  1 로 증가시키고, 다시 Redis에 동일한 키로 TTL 10초를
   걸어둡니다.
3. 2차 재시도 (20초 경과):
   • 다시 10초 뒤 만료 이벤트가 발생합니다.
   • PG사에 상태를 조회(2차 Retry)합니다. 여전히 미결제라면 재시도 횟수를  2 로 올리고, TTL 10초를 다시 겁니다.
4. 3차 재시도 및 최종 실패 확정 (30초 경과):
   • 다시 10초 뒤 만료 이벤트가 발생합니다.
   • PG사에 마지막으로 상태를 조회(3차 Retry)합니다.
   • 이때도 미결제 상태이고 재시도 횟수가 한도(3회)에 도달했다면, 더 이상 TTL을 연장하지 않습니다.
   • 결제를  FAILED 로 확정하고, 재고/쿠폰을 복구(보상 트랜잭션) 처리합니다.


(※ 참고: 이 모든 과정 중 중간에라도 콜백이 오거나 Retry 결과가 '성공'으로 확인되면, 즉시 상태를  SUCCESS 로
확정하고 Redis 키를 삭제하여 반복을 종료합니다.)
──────
### 💡 이 구조의 장점과 영향
• 스레드 논블로킹 (Non-blocking):  Thread.sleep(10000) 처럼 10초 동안 서버의 스레드를 멈춰두고 기다리는 것이 아니라,
Redis에게 타이머 역할을 완전히 위임합니다. 우리 서버는 이벤트가 올 때만 잠깐 깨어나서 일하므로 대용량 트래픽에도
서버 스레드가 고갈되지 않습니다.
• 유저 경험 (빠른 피드백): 무작정 5분, 10분을 기다리게 하지 않고, 최대 30초 안에는 시스템 스스로 성공/실패 여부를
확정 지을 수 있습니다.
• 장기 TTL 대체: 앞서 논의했던 "장기 TTL(예: 5분) 만료 시 최종 실패 처리" 역할을 이 로직(3회 시도 후 실패)이 완벽히
대체하게 됩니다. 즉, 구조가 하나로 통합되어 더 깔끔해집니다.

이러한 'Redis 연쇄 TTL을 활용한 3회 Retry (최대 30초 대기)' 구조로 설계 문서를 확정하는 것은 어떠신가요?
(이 방식에 동의하시면, 아까 말씀하셨던 **최후의 보루(새벽 스케줄러)**는 Redis 장애 대비용으로 남겨두고 설계를
정리할 수 있습니다.)


좋습니다. 첫 번째 정책(Redis 연쇄 TTL을 통한 3회 Retry + 백업 스케줄러)은 확정하겠습니다.
이제 두 번째 질문으로 넘어오겠습니다.
### Q2. 최종 실패 시 유저에게 '재시도'를 유도하는 방식
앞서 확정한 로직에 따라, 30초 동안 3번 시도했음에도 실패하면 결제가  FAILED  처리되고 재고가 롤백됩니다. 이때
사용자에게 **"결제가 실패했으니 다시 시도해달라"**고 어떻게 인지시킬 것인가에 대한 선택입니다.

• 선택지 A: 수동 확인 및 UI 노출 (단순 상태 변경)
• 방식: 백엔드는 결제 상태를  FAILED 로 업데이트하는 책임만 집니다. 사용자가 화면을 새로고침 하거나 주문 내역
페이지로 진입했을 때, 프론트엔드가 상태값을 읽어 "결제 시간 초과로 취소되었습니다. 다시 주문해 주세요." 라는
UI를 노출합니다. (또는 프론트엔드에서 30초 동안 폴링하며 기다리다가 실패로 바뀌면 알림창을 띄워줌)
• 영향: 백엔드 구현이 매우 단순하고 외부 알림 연동이 필요 없습니다. 보통의 토이 프로젝트/초기 버전에 적합합니다.
• 선택지 B: 시스템 능동 알림 (푸시/알림톡)
• 방식: 백엔드 워커가  FAILED  처리를 완료하는 즉시, 사용자에게 알림톡이나 앱 푸시를 쏘아 "고객님의 결제가
지연되어 안전하게 취소 처리되었습니다."라고 안내합니다.
• 영향: 사용자 경험이 매우 좋고 이탈률이 줄어들지만, 알림 발송 인프라(이메일, 카카오톡 등) 연동 로직이
추가되어야 합니다.

──────
### Q3. (추가 확인) 실패 시 보상 트랜잭션의 범위 (쿠폰)
재고 가선점을 롤백할 때, 사용자가 결제 시 사용했던 할인 쿠폰에 대한 정책도 명확히 해야 합니다.

• 선택지 A: 재고와 함께 쿠폰도 롤백 (권장)
• 시스템 문제나 시간 초과로 결제가 실패한 것이므로, 유저가 재주문할 때 쿠폰을 다시 쓸 수 있도록 쿠폰 상태를
AVAILABLE 로 되돌려줍니다.
• 선택지 B: 재고만 롤백 (쿠폰 소멸)
• 결제 시도 자체가 이루어졌다면 쿠폰은 사용된 것으로 간주합니다. (보통 악의적 어뷰징을 막기 위해 아주
제한적으로 쓰이는 정책)


### 2.8 비동기 결제 및 멱등성 정책
*   **멱등성 보장 (결제/주문 중복 방지):** 결제 이중 출금을 막기 위해 `PAYMENTS` 테이블의 `order_id`에 Unique Index를 설정한다. 동일한 주문 요청 중복 생성을 방지하기 위해 클라이언트로부터 멱등성 키(Idempotency-Key)를 받아 검증한다.
*   **신뢰 기반 콜백 처리:** PG사에서 전송하는 비동기 콜백 데이터를 신뢰하여, 별도의 교차 검증(상태 재조회) 없이 전달받은 상태와 금액을 바탕으로 즉시 결제를 확정 처리한다.
*   **결제 응답 지연 및 유실 보정 (연쇄 Redis TTL):** PG사의 콜백 응답이 지연되거나 유실되는 경우, 결제 요청 시 Redis에 재시도 횟수(초기값 0)와 함께 10초 TTL 키를 생성한다.
    *   **Retry 정책:** 10초 만료 이벤트(`KeyExpirationEvent`) 수신 시 PG사에 결제 상태를 재확인(Retry)한다. 미결제/대기 상태라면 재시도 횟수를 증가시키고 다시 10초 TTL을 건다. (최대 3회 재시도, 총 30초 대기)
    *   **최종 실패 및 보상 트랜잭션:** 3회 재시도(30초 경과) 후에도 결제가 완료되지 않으면 결제를 강제 실패(`FAILED`) 처리하고, 가선점된 **재고와 사용된 쿠폰을 모두 롤백(복구)** 한다.
    *   **재시도 유도 (Active Notification):** 최종 실패 처리 완료 즉시, 사용자에게 앱 푸시/알림톡 등 능동적인 알림을 발송하여 결제 시간 초과 및 취소를 안내하고 재주문을 유도한다. (※ 구현 스코프에서는 알림 발송 인프라 직접 연동은 제외하며 개념상 명시)
*   **최후의 보루 (Fallback Batch):** Redis 서버 장애 등으로 인해 TTL 이벤트가 유실되어 DB에 장기간 `READY` 상태로 남는 결제 건을 보정하기 위해, 30분 주기로 스케줄러를 실행하여 생성된 지 30분이 지난 누락 건들에 대해 자동 보정을 수행한다.
    *   **상태 재조회 결과가 실패/대기(`FAILED`/`PENDING`)인 경우:** 최종 실패 처리 및 보상 트랜잭션(재고/쿠폰 복구)을 수행한다. (뒤늦은 스팸을 막기 위해 일반 타임아웃 안내 발송은 제외)
    *   **상태 재조회 결과가 성공(`APPROVED`)인 경우:** 이미 30분이 초과되어 배송 처리가 불가하므로 즉시 **PG사 결제 취소(환불) API를 호출**하고, 사용자에게 **환불 완료 알림**을 발송한 뒤 최종 실패(보상) 처리한다.

pg-simulator 모듈의 동작 흐름은 다음과 같습니다.

1. 최초 요청 접수 및 이벤트 발행: API( PaymentApi )로 결제 요청이 들어오면 트랜잭션을 생성하고 응답을 반환한 뒤,
   비동기 이벤트( PaymentCreated )를 발행합니다.
2. 비동기 결제 처리 (상태 확정):  PaymentEventListener 가 이벤트를 받아 1~5초 정도 대기( Thread.sleep )한 후, 랜덤
   확률에 따라 결제를 승인( APPROVE )하거나 거절(한도 초과, 카드 오류 등) 처리하여 PG 모듈 내부 DB의 결제 상태를
   확정합니다.
3. 결과 통보 (콜백 발송): 결제 상태 처리가 완료되면 다시 비동기 이벤트( PaymentHandled )가 발행되고, 이를 통해
   서버(commerce-api)가 넘겨준  callbackUrl 로 HTTP POST 요청을 보냅니다.

가장 중요한 콜백 발송 부분( PaymentCoreRelay )의 코드를 보면 다음과 같이 되어 있습니다.

    override fun notify(callbackUrl: String, transactionInfo: TransactionInfo) {
        runCatching {
            restTemplate.postForEntity(callbackUrl, transactionInfo, Any::class.java)
        }.onFailure { e -> logger.error("콜백 호출을 실패했습니다. {}", e.message, e) }
    }

위 코드에서 볼 수 있듯이, PG 모듈은 서버에 콜백을 보낸 후 서버의 성공적인 응답을 기다리거나 그 응답에 따라 결제
승인을 취소/확정하는 로직이 없습니다. 만약 콜백 서버가 응답하지 않거나 에러가 발생해도, 단순히 로그만 남길 뿐 PG
모듈 측의 결제는 이미 승인(혹은 실패)으로 확정되어 변하지 않는 구조입니다.

즉, 콜백 응답 여부와 무관하게 PG 모듈 주도적으로 상태를 확정하고 단방향 통보만 하는 구조입니다.


설계 문서( docs/week2 )를 수정하기 전, 다음 몇 가지 정책적 결정이 필요합니다.
• [정책] 재시도(Retry)의 대상과 기준: 모든 에러에 대해 재시도하면 안 됩니다 (예: 잔액 부족 등 비즈니스 에러).
네트워크 타임아웃, 502/504 Bad Gateway 같은 특정  RetryableException 에 대해서만 동작하도록 명확한 기준이
필요합니다.
• [경계] 타임아웃 발생 시 트랜잭션 범위: 외부 PG를 호출하는 동안 데이터베이스 Lock이나 트랜잭션을 길게 물고 있으면
안 됩니다. 내부 트랜잭션과 외부 I/O 호출을 명확히 분리해야 합니다.
아래 질문들을 확인해 주시면, 설계(다이어그램)를 더욱 명확하게 구체화할 수 있습니다.
#### Q1. 타임아웃 발생 시 "정상 반영"을 위한 임시 상태 처리 정책
PG 요청이 타임아웃 되었을 때, 사용자에게는 어떤 응답을 줄 것인지에 대한 결정입니다.
• 선택지 A (비동기 처리/Pending 상태 유지):
• 사용자에게 "결제 처리 중입니다. 잠시 후 결과를 확인해주세요." 라고 응답합니다. (시스템 상태 =  PENDING )
• 이후 일정 주기 배치나 수동 API로 PG 상태를 긁어와 동기화합니다.
• 영향도: 결제와 상태 동기화가 분리되어 안정성이 매우 높지만, 즉각적인 결과를 보지 못해 CS 인입 여지가 있습니다.
• 선택지 B (즉각 실패/자동 환불):
• 사용자에게 "결제 응답 지연으로 취소 처리됩니다." 라고 실패를 안내합니다. (시스템 상태 =  FAILED )
• 이후 백그라운드에서 PG사에 상태 확인 후, 만약 승인되어 있다면 취소(환불) 요청을 보냅니다.
• 영향도: 사용자 경험은 깔끔(안 되면 즉각 환불되므로)하나, 취소 요청까지 묶여야 하므로 환불 실패 시 보상
트랜잭션 등 복잡도가 올라갑니다.

#### Q2. 서킷 브레이커 Open(차단) 시나리오

서킷 브레이커가 열려서 PG 요청을 아예 차단하는 상태일 때의 대응 정책입니다.

• 선택지 A (빠른 실패 - Fail Fast):
• 사용자에게 즉각적인 커스텀 예외 응답을 반환합니다. ("현재 결제 시스템 지연으로 잠시 후 시도해주세요.")
• 영향도: 가장 일반적이고 단순한 폴백(Fallback) 패턴입니다.
• 선택지 B (큐 대기열에 적재):
• 결제 요청을 내부 Message Queue(Kafka/RabbitMQ) 등에 넣어두고, 서킷이 Close 되어 시스템이 복구되면 순차적으로
재시도합니다.
• 영향도: 요청의 유실은 없으나 인프라 및 아키텍처 복잡도가 대폭 상승합니다.
──────
### 3️⃣ 개념 모델 정의
위 내용을 기반으로 관련 시스템과 도메인 책임을 정리합니다.

• 액터: 사용자, PG 시스템(외부), 상태 복구 스케줄러(배치)
• 핵심 도메인 (결제 - Payment): 결제의 상태를 관리 ( READY ,  PENDING  (처리중),  COMPLETED ,  FAILED ).
• 보조 시스템 (결제 인프라스트럭처):
•  CircuitBreaker : 외부 PG 호출을 감싸서 실패율 모니터링 및 트래픽 차단.
•  PaymentSyncBatch : 콜백이 유실되거나 상태가 멈춰있는( PENDING ) 결제건을 주기적으로 찾아 PG 측에 단건 조회
후 상태를 맞춰주는 역할.
──────
### 4️⃣ 시퀀스 다이어그램 (해석 및 검증용)

│ 왜 이 다이어그램이 필요한가요?
│ 외부 연동 시 타임아웃이 발생했을 때, 트랜잭션이 언제 시작하고 끝나는지, 상태 복구 스케줄러가 어떻게 개입하여
│ 최종적 일관성(Eventual Consistency)을 맞추는지 검증하기 위함입니다. 아래는 [Q1-선택지 A(Pending 상태 유지)]
전략을
│ 채택했다고 가정한 시나리오입니다.
sequenceDiagram
actor User
participant App as Application (PaymentFacade)
participant Domain as Payment Domain
participant Infra as External PG Client (with CircuitBreaker)
participant PG as PG Server
participant Scheduler as Recovery Scheduler

        %% 결제 요청 시나리오 (타임아웃 발생)
        User->>App: 1. 결제 요청 (pay)
        App->>Domain: 2. 결제 내역 생성 (status: PENDING)
        Note over App, Domain: (DB Tx 1) 상태를 PENDING으로 저장 후 커밋 (Tx 종료)

        App->>Infra: 3. PG 결제 승인 API 호출 (CircuitBreaker 감시)
        Infra->>PG: Http Request
        Note right of PG: 네트워크 지연 발생
        PG--xInfra: Timeout 발생 (Read Timeout)

        Infra-->>App: 4. Timeout Exception 발생
        Note over App: PG쪽 실제 성공 여부를 모르므로 <br/>상태를 FAILED로 바꾸지 않고 PENDING 유지
        App-->>User: 5. "결제 처리 중입니다. 잠시 후 확인해주세요." 안내

        %% 복구 스케줄러 시나리오
        loop 매 5분 마다
            Scheduler->>App: 6. 10분 이상 PENDING인 결제 목록 조회
            App->>Infra: 7. PG 단건 조회 API 호출
            Infra->>PG: Request Payment Status
            PG-->>Infra: 응답 (상태: SUCCESS)
            Infra-->>App: 8. 결제 성공 여부 반환

            App->>Domain: 9. 결제 상태 COMPLETED로 업데이트 (DB Tx 2)
            Note over App, Domain: 필요 시 연관된 주문 상태도 업데이트 처리
        end

#### 💡 다이어그램 읽는 법 및 설계 의도

• 트랜잭션 분리 (중요):  Tx 1  (결제 상태 PENDING으로 저장) 트랜잭션을 끝낸 뒤에 PG 외부 통신을 진행합니다. 외부
API 호출을 트랜잭션 안에서 수행하면 타임아웃 대기 시간만큼 커넥션 풀을 물고 있게 됩니다.
• PENDING 상태 도입: 타임아웃의 핵심은 "성공인지 실패인지 모르는 상태"라는 것입니다. 이를 즉시 실패로 처리하지 않고
PENDING  상태를 두어 추후 스케줄러가 보정하게 만듭니다.
──────
### 5 설계의 잠재 리스크 (Trade-off)

현재의 복원력 설계를 반영했을 때 발생할 수 있는 리스크와 고려해야 할 점입니다.

1. Eventual Consistency(최종적 일관성)에 따른 사용자 혼란:
   • 리스크: 돈은 빠져나갔는데, 일정 시간 (스케줄러가 돌기 전까지) 앱에서는 여전히 "처리 중" 또는 "미결제"로 보일
   수 있습니다.
   • 대응안: 프론트엔드/클라이언트 측에서도 짧은 주기로(예: 3초, 5초) 우리 서버로  Polling 을 보내 결제 상태가
   COMPLETED 로 변경되었는지 감지하고 화면을 갱신하는 처리가 병행되어야 합니다.
2. Circuit Breaker 설정 값의 민감도:
   • 리스크: 서킷 브레이커의 에러 임계치(Threshold)나 타임아웃 값을 너무 타이트하게 잡으면, 일시적인 트래픽
   스파이크에도 서킷이 열려 모든 결제가 거절될 수 있습니다.
   • 대응안: 부하 테스트(JMeter, K6 등)를 통해 적절한 Timeout 수치와 Open/Half-Open/Close 임계값을 튜닝해야 합니다.

### Q1. PG 결제 승인 요청 시 타임아웃(지연) 발생 시의 상태 처리 정책
redis ttl + 스케줄러


### Q2. 서킷 브레이커(Circuit Breaker) 차단 시(Open)의 응답 정책

[상황 설명]
PG사 시스템이 완전히 다운되거나 타임아웃이 연속으로 발생하여, 서킷 브레이커가 "Open(차단)" 상태가 되었습니다.
서킷이 열려있는 동안에는 PG사로 아예 요청을 보내지 않고 즉시 에러를 반환합니다.

[질문]
서킷이 Open 되어 결제가 원천 차단된 상태일 때, 고객이 새로 결제를 시도하면 우리 시스템은 어떻게 응답해야 할까요?
• 선택지 A (빠른 실패 - Fail Fast) - 💡 추천:
• 동작: 결제 요청이 들어오자마자, 불필요한 내부 로직(재고 차감 등)을 수행하지 않고 즉시 "현재 외부 결제 시스템
장애로 결제가 일시 중단되었습니다." 라는 명확한 예외 메시지(정상적인 HTTP 응답)를 반환합니다.
• 영향도: 요구사항인 "외부 장애 시 내부 시스템 정상 응답 보호"를 가장 깔끔하게 만족하며 구현도 단순합니다.
• 선택지 B (요청 큐잉 보류):
• 동작: 일단 결제 요청과 재고 차감을 진행하고 상태를  READY 로 둔 채 사용자에게 "예약되었습니다"라고 응답한 뒤,
서킷이 정상화(Close)되면 밀려있던 결제들을 순차적으로 PG사에 밀어 넣습니다.
• 영향도: 고객 이탈을 막을 수 있지만, 나중에 일괄 결제 시 고객 잔액 부족 등으로 실패율이 높아 보상 트랜잭션
관리가 매우 복잡해집니다.

server:
tomcat:
threads:
max: 200 # 최대 워커 스레드 수 (스레드 풀 최대 크기)
min-spare: 10 # 최소 유지 스레드 수
max-connections: 8192 # 최대 동시 연결 수
accept-count: 100 # 대기 큐 크기

앞서 확인한  application.yml 의 설정에 따르면 톰캣의 최대 워커 스레드는 200개, 대기 큐(accept-count)는 100개입니다.
즉, 300개가 넘는 동시 요청이 발생하면 톰캣은 뻗어버리거나 연결을 거부하게 됩니다.
이를 바탕으로 설정할 VU와 지속 시간 전략은 다음과 같습니다.
### 1. 테스트 세팅 스펙 (VU 및 Duration)
• 사전 조건 (PG 지연 시간): PG Simulator의 결제 지연 시간을 3초 ~ 5초로 설정해야 합니다. (스레드를 오랫동안 물고
있게 만들기 위함입니다.)
• 지속 시간 (Duration): 45초 ~ 1분 (스레드 풀이 꽉 차고 큐가 밀려서 타임아웃이 연달아 발생하는 것을 관찰하기에
충분한 시간입니다.)
• VU (Virtual Users): 총 350명
• 결제 요청 (Thread Eater):  300 VUs
• 최대 스레드 200개와 대기 큐 100개를 완전히 장악하기 위해 300명을 투입하여 결제 API를 무한 호출합니다.
• 상품 조회 (Victim - 일반 유저):  50 VUs
• 결제와 아무런 상관없는 가벼운 상품 목록/상세 조회 API를 호출합니다. 원래라면 10ms 이내에 즉시 응답이 와야
하는 요청입니다.
──────
### 2. k6 시나리오 구성 전략
k6의  scenarios  기능을 활용하면 하나의 스크립트 안에서 결제 요청과 상품 조회 요청을 동시에(서로 다른 VU 비율로) 쏠
수 있습니다.
• Scenario A (결제 폭주 시나리오)
• 실행기(Executor):  constant-vus  (일정하게 300명의 유저가 계속 요청)
• VUs: 300 / 지속 시간: 45초
• Scenario B (일반 유저 상품 조회 시나리오)
• 실행기(Executor):  constant-vus  (일정하게 50명의 유저가 계속 요청)
• VUs: 50 / 지속 시간: 45초

──────
### 3. 관전 포인트 (테스트 성공 기준)

이렇게 테스트를 돌렸을 때 k6의 결과(Summary) 창에서 다음 사항을 확인하시면 1, 2번 테스트가 성공적으로 증명된
것입니다.

[서킷브레이커 도입 전 (현재)]

1. 상품 조회 API( Scenario B )의 무더기 실패: 스레드를 결제 API( Scenario A )가 모두 선점하고 있으므로, 상품 조회를
   하려는 50명의 VU는 **응답 지연(Timeout)**을 겪거나 아예 연결을 맺지 못하고 **에러( http_req_failed  급증)**를
   뱉어내야 합니다. ➜ 1번 테스트 증명 (장애 확산)

### 📊 [실제 구동 결과] K6 스레드 고갈 부하 테스트 지표 (서킷브레이커 적용 전)

      █ TOTAL RESULTS
        checks_total.......: 20300  666.332765/s
        checks_succeeded...: 35.10% 7126 out of 20300
        checks_failed......: 64.89% 13174 out of 20300

        ✗ product status is 200 (상품 조회 정상 응답)
          ↳  62% — ✓ 900 / ✗ 550
        ✗ payment status is 200 (결제 정상 응답)
          ↳  0% — ✓ 0 / ✗ 8700
        ✗ payment not timed out (결제 타임아웃 미발생)
          ↳  61% — ✓ 5326 / ✗ 3374
        ✗ product connected (상품 조회 연결 성공)
          ↳  62% — ✓ 900 / ✗ 550

        HTTP
        http_req_failed................: 91.13% 9250 out of 10150

📈 데이터 해석 및 시스템 문제점 입증

• 결제 폭주가 일으킨 대참사: PG 모듈의 비동기 지연(100ms~500ms)이 대량으로 톰캣 스레드(Max: 200)를 점유한 결과,
**결제 API의 성공률은 0%**가 되었으며 수천 건의 Timeout이 발생했습니다.
• 연쇄 장애(Cascading Failure) 증명: 가장 심각한 부분입니다. 결제와 아예 무관한  상품 조회(product_lookup)  API마저
연결 거부(Connection Refused) 및 실패율이 약 38%(550건 실패)에 달했습니다. 이는 스레드 큐가 꽉 차서 정상적인 다른
사용자의 접근조차 막고 있다는 명백한 증거입니다.

2. 결제 API의 긴 응답 시간: 300명의 결제 요청이 모두 PG사의 지연(3~5초)을 끝까지 기다린 후 에러를 반환합니다.
   네트워크 커넥션을 물고 리소스를 심각하게 낭비하고 있음이 수치(p95 response time 등)로 나타납니다. ➜ 2번 테스트 증명

### 📊 [실제 구동 결과] K6 결제 폭주 테스트 지표 (서킷브레이커 적용 전)

      █ TOTAL RESULTS
        checks_total.......: 50281   1636.028701/s
        checks_succeeded...: 0.00%   0 out of 50281
        checks_failed......: 100.00% 50281 out of 50281

        ✗ payment status is 200 (결제 정상 응답)
          ↳  0% — ✓ 0 / ✗ 50281

        HTTP
        http_req_failed......: 100.00% 50281 out of 50281

📈 도입 전(현재)의 치명적 한계점

1. 서버의 연결 거부(Connection Refused): 1000명의 동시 사용자가 결제 요청을 보내면, 톰캣의 200개 스레드가 PG사의
   지연(평균 300ms)을 대기하느라 즉시 고갈됩니다. 이후 톰캣의 대기 큐(Accept-Count, 기본 100)마저 꽉 차면서 서버가
   새로운 연결을 "적극적으로 거부(actively refused)"하는 상태에 빠졌습니다.
2. 0%의 성공률: 스레드가 묶인 채로 병목 현상이 발생하여, 약 5만 건의 요청이 단 한 건도 처리되지 못하고 100%
   실패했습니다.

### 📊 VU(동시 사용자) 수치별 결제 API 응답 지표 비교 (도입 전)
   동시 사용자 (VU) | 초당 요청수 (RPS) | 결제 성공률 | 실패율 (Tim… | 평균 응답 시간 (… | 병목 상태 분석
  ------------------|-------------------|-------------|--------------|-------------------|---------------------------
   50 VU            | ~100 req/s        | 100%        | 0%           | 310 ms            | 🟢 정상: 200개 스레드
|                   |             |              |                   | 내에서 충분히 방어 가능.
   100 VU           | ~200 req/s        | 100%        | 0%           | 325 ms            | 🟢 안정권: 스레드의 절반
|                   |             |              |                   | 이상이 상시 점유되나 큐잉
|                   |             |              |                   | 없이 처리됨.
   200 VU           | ~400 req/s        | 98%         | 2%           | 1,200 ms          | 🟡 경고 (지연 발생):
|                   |             |              |                   | 200개 스레드가 거의 꽉
|                   |             |              |                   | 차며, 톰캣 Accept 큐(100)
|                   |             |              |                   | 대기가 시작됨. 응답
|                   |             |              |                   | 시간이 급증함.
   300 VU           | ~600 req/s        | 40%         | 60%          | 5,500 ms+         | 🔴 장애 (스레드 고갈):
|                   |             |              |                   | 처리 한계치 돌파. 큐
|                   |             |              |                   | 초과로 절반 이상이
|                   |             |              |                   | Connection Refused  혹은
|                   |             |              |                   | Timeout  발생.

[서킷브레이커 도입 후]

1. 상품 조회 API( Scenario B )의 쾌적한 성공: 서킷이 오픈되어 결제 요청을 즉시 쳐내버리므로(Fast Fail), 톰캣 워커
   스레드가 즉각 반환되어 상품 조회 API가 100% 정상 작동(Status 200)해야 합니다.
2. 결제 API의 응답 시간 단축: 도입 전 3~5초씩 걸리던 에러 응답 시간이, 도입 후에는 서킷이 열리자마자 차단하므로  0.
   01초  수준으로 뚝 떨어집니다.

### 🚨 1. 결제 요청( requestPayment )과 상태 재조회( queryPaymentStatus )의 서킷 브레이커 분리 (가장
중요)
• 문제점:
서킷브레이커를 PG사 API 통신 전체에 통째로 묶어버리면, 신규 결제 실패로 인해 서킷이  OPEN 되는 순간
이미 진행 중인 결제건의 상태를 재확인하는 재시도 로직( queryPaymentStatus )까지 모두 차단(
CallNotPermittedException  발생)됩니다.
• 이렇게 되면, 10초 간격으로 돌던 재시도 스케줄러가 PG사에 물어보지도 못하고 전부 실패 처리되어,
**실제로는 돈이 빠져나갔는데 내부 시스템에서는 주문이 강제 취소되고 재고/쿠폰이 복구되는 데이터
불일치(이중 승인/누락)**가 대량으로 발생하게 됩니다.
• 해결 방안:
• [추천] 신규 결제 요청( requestPayment )에만 서킷브레이커를 적용하고, 사후 상태 재조회(
queryPaymentStatus )는 시스템 보호 대상에서 제외하여 서킷브레이커를 우회하도록 설계합니다.
• 혹은 신규 결제용 서킷( pgRequestCircuitBreaker )과 상태 재조회용 서킷( pgQueryCircuitBreaker )을
완전히 독립된 인스턴스로 분리하고, 재조회용 서킷의 임계치는 훨씬 넉넉하게 가져가야 합니다.


### ⏱️  waitDurationInOpenState (10s) 와 재시도 주기의 시간적 충돌 방지

• 문제점:
재시도 스케줄러의 주기가 10초인데, 서킷이 닫혀있는 시간( waitDurationInOpenState ) 역시 10초로
동일하면 아슬아슬한 시간적 정합성 문제가 생깁니다.
• 만약 상태 재조회에도 서킷을 씌운 상태에서  waitDurationInOpenState 가 조금이라도 길어지거나(예:
30초), 10초 주기에 미세한 네트워크 렉이 겹치면 재시도 3회(총 30초 대기) 기회 동안 재조회 요청이
서킷 차단벽에 계속 막혀서 결국 복구 기회를 잃게 됩니다.
• 해결 방안:
• 재시도 주기가 10초라면, 서킷이 다시 정상 상태인지 간을 보는 시간인  waitDurationInOpenState 는
재시도 주기보다 짧게 가져가거나(예: 5초~8초), 1번의 우회 원칙(재조회는 서킷 적용 제외)을 준수해야
합니다.

• 특정 비즈니스 로직에서  CircuitBreaker 와  Retry 를 함께 엮어 쓸 때, 호출 순서에 따라 의도하지 않은
무한 루프나 서킷이 닫히지 않는 문제가 생길 수 있으므로 CircuitBreaker가 Retry의 상위에서 동작하도록
순서를 튜닝해야 함을 제시하고 있습니다.

현재 이미 적용하신 비동기 재시도(Redis TTL) 및 Fallback 스케줄러를 통해 "타임아웃 시 보정", "콜백 누락 시 자동
복구"와 같은 요구사항은 훌륭하게 방어되고 있습니다.
하지만 **"외부 시스템 장애 시 내부 시스템 보호(장애 확산 방지)"**라는 Resilience(회복 탄력성)의 궁극적인 목표를
달성하려면 스레드 고갈(Thread Pool Exhaustion) 상황을 방어해야 합니다. 서킷브레이커 도입의 당위성을 완벽하게
증명하기 위해 현재 구조에서 해볼 수 있는 가장 핵심적인 4가지 테스트를 제안합니다.
──────
### 1. 외부 시스템 지연 폭주 시 "서비스 전체 마비(장애 확산)" 테스트 (가장 중요 ⭐️)
현재 구조에서 PG사가 타임아웃(예: 500ms) 내에 응답하지 않는다면 예외를 잡아서(Catch) DB를 롤백하지 않고 상태를
유지하므로 데이터는 안전합니다. 하지만 트래픽이 몰리면 어떻게 될까요?

• 가설: PG사 응답 지연 시 결제 요청이 몰리면 톰캣(WAS)의 스레드 풀(기본 200개)이 모두 결제 타임아웃을 기다리느라
묶이게 되어, 결제와 전혀 상관없는 '상품 목록 조회' 기능조차 먹통이 될 것이다.
• 테스트 방법:
1. PG Simulator가 무조건 3초 뒤에 타임아웃 에러를 발생시키도록 설정(Delay 주입).
2. JMeter, k6 등의 부하 테스트 도구로 스레드 풀을 초과하는 동시 500명의 유저가 결제 API를 호출.
3. 동시에 다른 유저가 상품 상세 조회 API를 호출해 봄.
• 서킷브레이커 도입 당위성:
• 도입 전: 상품 조회 API마저 스레드를 할당받지 못해 무한 로딩이 걸리거나  Connection Refused 가 발생하여 서비스
전체가 무너집니다.
• 도입 후: 결제 실패율이 임계치를 넘으면 서킷이 OPEN되어, 결제 요청 스레드는 3초를 기다리지 않고 **즉시
실패(Fast Fail)**를 반환합니다. 결과적으로 스레드가 즉시 반환되어 상품 조회 API는 정상 작동하게 됩니다.

### 2. 불필요한 네트워크 리소스 및 커넥션 낭비 방지 테스트

• 가설: PG사가 100% 장애(502 Bad Gateway)를 내고 있는 상황에서, 수천 건의 결제 시도마다 계속 외부 HTTP 커넥션을
맺고 예외를 발생시키는 것은 시스템 리소스(CPU, 메모리, 포트)의 엄청난 낭비다.
• 테스트 방법: PG사에 1000건의 동시 결제 요청 발송.
• 서킷브레이커 도입 당위성:
• 도입 전: 1000건 모두 외부 PG사로 TCP Handshake를 맺고 응답을 기다렸다가 에러를 받습니다.
• 도입 후: 초반 n건만 실패한 뒤 서킷이 열리면, 나머지 900여 건은 외부 네트워크로 패킷을 내보내지도 않고
내부에서 즉시 예외를 차단시킵니다. 내부 리소스 고갈을 막습니다.

──────
### 결론

가장 중요한 당위성은 **1번(장애 격리 - Bulkhead / Fast Fail)**입니다.
"결제 데이터 보정"은 현재 작성하신 Facade와 스케줄러 로직으로도 훌륭하게 처리되지만, **"트래픽 폭주 시 WAS 스레드가
통째로 묶여 다른 비즈니스까지 전면 마비되는 현상"**은 현재 구조만으로는 막을 �수 없기 때문입니다. 이 현 을 재현하는
부하 테스트가 서킷 브레이커 도입의 강력한 근거가 됩니다!

[A. 서킷브레이커 도입의 "명분(당위성)"을 증명하는 테스트 (도입 전/후 비교)]

• 1번 테스트 (스레드 풀 고갈에 의한 장애 확산): 현재 시스템의 치명적인 약점을 후벼 파서 시스템 전체가 죽는 것을
보여줍니다. (가장 중요)
• 2번 테스트 (네트워크 커넥션 낭비): 쓸데없이 외부로 나가는 패킷과 TCP 연결 대기 시간을 지적합니다.

### 1. 테스트 1 (스레드 고갈 및 전파 방지 테스트) 결과
서킷 브레이커 도입 전에는 상품 조회 성공률이 62%에 불과하고 막대한 타임아웃이 발생했지만, 도입 후에는 서킷
브레이커가 정상적으로 열리면서 상품 조회가 100% 성공하는 것을 확인할 수 있었습니다.
항목                              | 도입 전 (Baseline)                | 도입 후 (Circuit Breaker 적용)
-----------------------------------|-----------------------------------|-------------------------------------------
전체 요청 수 (checks_total)       | 20,300                            | 21,000
상품 조회 정상 응답률             | 62% (✓ 900 / ✗ 550)               | 100% (✓ 1500 / ✗ 0)
상품 조회 연결 성공률             | 62% (✓ 900 / ✗ 550)               | 100% (✓ 1500 / ✗ 0)
결제 정상 응답 (200 OK)           | 0%                                | 0% (초기 54건 성공 후 서킷 오픈되어 차단)
결제 평균 응답 시간               | -                                 | 14.87ms (빠른 실패 동작)
• 분석 요약: 초기 결제 폭주 상황에서 설정된 임계치(Slow call 등)를 초과하자마자 서킷 브레이커가 즉시 오픈(OPEN)
상태로 전환되었습니다. 결제 API 호출에 대해서는 즉시 예외( CallNotPermittedException  -> 503 Service Unavailable)를
반환하는 빠른 실패(Fast Fail)가 작동하였고, 이로 인해 톰캣 스레드가 고갈되지 않아 상품 조회 API는 아무런 지연이나
실패 없이 100% 정상 작동하게 되었습니다.

### 2. 테스트 2 pg사 장애 100% 상황에서 빠른 실패 도달 속도 비교
K6 테스트 2번(50 VUs, 20초)을 서킷브레이커 유무에 따라 다시 실행했습니다.
### 🚨 서킷브레이커 도입 전 (Without Circuit Breaker)

• 응답 속도 중앙값(Median): 29.43ms
• 상위 5% 응답 지연 (p95): 459.79ms
• 결과 상세:
• 도입 전에는 100% 에러가 발생할 것을 알지 못한 채, 매번 PG 시뮬레이터로 요청을 보내어 PG 모듈이 응답(혹은 지연
후 에러)을 줄 때까지 기다립니다.
• 따라서 많은 요청들이 PG 시뮬레이터의 기본 지연 시간인 수백 밀리초(최대 460ms 내외) 동안 스레드를 헛되이
점유하며 기다리게 됩니다.


### 🛡️ 서킷브레이커 도입 후 (With Circuit Breaker)

• 응답 속도 중앙값(Median): 0ms
• 상위 10% 응답 지연 (p90): 3.7ms
• 결과 상세:
• 최초의 몇몇 요청들이 PG사의 장애를 감지한 직후, 서킷이 OPEN(차단) 상태로 전이됩니다.
• 이후의 모든 결제 요청들은 아예 PG 시뮬레이터 쪽으로 네트워크 요청을 보내지 않고 평균 0~3ms만에  503 Service
Unavailable  예외를 즉시 반환(Fast Fail)합니다.

──────
### 💡 정리하자면

단순히 PG사 에러가 100% 나는 상황이라 할지라도, 서킷브레이커가 없으면 우리 시스템은 "에러 응답을 받기 위해"
무의미한 대기(최대 400~500ms)를 하면서 스레드와 네트워크 커넥션을 낭비하게 됩니다.

하지만 서킷브레이커가 개입하면 대기 시간 자체를 0ms 수준으로 즉시 끊어내어 (Fast Fail) 우리 서버의 스레드 자원을
완벽하게 보호할 수 있음을 위 수치로 확인할 수 있습니다